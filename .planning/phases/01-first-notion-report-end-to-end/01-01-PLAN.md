---
phase: 01-first-notion-report-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/04_data_health_check.sql
  - sql/02_top_full_length_videos.sql
  - sql/01_latest_snapshot_overview.sql
  - sql/03_age_controlled_performance.sql
  - docs/runbook.md
  - .gitignore
  - runs/README.md
  - .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md
  - CHANGELOG.md
  - .planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md
autonomous: false
requirements:
  - HEALTH-02
  - BQ-03
  - NOTION-04
user_setup: []

must_haves:
  truths:
    - "Every BigQuery query in sql/ uses CURRENT_DATE('America/Phoenix'); no bare CURRENT_DATE() remains."
    - "docs/runbook.md has zero references to BUSINESS_RULES.md §5 or §6; every cross-reference resolves to §1–§4."
    - ".gitignore commits .claude/skills/write-notion-report/** while still ignoring other .claude/skills/ children."
    - "runs/README.md documents the additive transport and notion_url fields that summary.json will carry."
    - "The BigQuery MCP execute_sql_readonly argument key names and the Notion notion-fetch behavior against NOTION_REPORT_PAGE_ID are both probed and recorded in 01-PROBE-NOTES.md."
    - "01-CONTEXT.md no longer says NOTION_PAGE_ID; every occurrence reads NOTION_REPORT_PAGE_ID (env-var reconciliation per RESEARCH §9, §10 Q1)."
  artifacts:
    - path: ".planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md"
      provides: "Recorded argument shape for BigQuery MCP execute_sql_readonly and result of notion-fetch preflight"
      contains: "execute_sql_readonly"
    - path: "CHANGELOG.md"
      provides: "Single dated entry covering the timezone fix, runbook §5/§6 fix, env-var name reconciliation, and summary.json schema addendum"
      contains: "America/Phoenix"
  key_links:
    - from: "sql/04_data_health_check.sql"
      to: "BUSINESS_RULES.md §3 staleness rule"
      via: "CURRENT_DATE('America/Phoenix')"
      pattern: "CURRENT_DATE\\('America/Phoenix'\\)"
    - from: ".gitignore"
      to: ".claude/skills/write-notion-report/SKILL.md"
      via: "double-negation pattern (!dir/ then !dir/**)"
      pattern: "!\\.claude/skills/write-notion-report/"
---

<objective>
Reconcile environment-variable naming, fix the three scaffold defects that would otherwise corrupt Phase 1 outcomes, probe the two MCP shapes the recipe and Skill will depend on, and adjust `.gitignore` so the Skill committed in Plan 02 actually ships.

Purpose: Plans 02 and 03 build on top of these touch points (SQL files, env var name, summary.json schema, MCP shapes, gitignore). Doing the fixups here keeps those plans focused on the recipe and the Skill, not on reconciliation work.

Output: Corrected SQL/runbook/gitignore/runs-README, an updated CONTEXT.md, a single dated CHANGELOG entry, and a probe-notes file Plans 02 and 03 read before generating MCP-tool invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md
@.planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@BUSINESS_RULES.md
@CLAUDE.md
@sql/04_data_health_check.sql
@sql/02_top_full_length_videos.sql
@sql/01_latest_snapshot_overview.sql
@sql/03_age_controlled_performance.sql
@docs/runbook.md
@runs/README.md
@.env.example
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CURRENT_DATE timezone, BUSINESS_RULES section refs, and env-var name in scaffold</name>
  <files>sql/04_data_health_check.sql, sql/02_top_full_length_videos.sql, sql/01_latest_snapshot_overview.sql, sql/03_age_controlled_performance.sql, docs/runbook.md, .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md, CHANGELOG.md</files>
  <read_first>
    - sql/04_data_health_check.sql (lines 14–42 contain the four UNION ALL blocks using bare CURRENT_DATE())
    - sql/02_top_full_length_videos.sql (line 20 uses bare CURRENT_DATE() for days_since_published; line 4 references BUSINESS_RULES.md §3 incorrectly — leave that ref alone, only the timezone changes here)
    - sql/01_latest_snapshot_overview.sql (no CURRENT_DATE in body — confirm by reading; if absent, do NOT edit this file beyond confirming)
    - sql/03_age_controlled_performance.sql (per CONCERNS.md, lines 24 and 35 use bare CURRENT_DATE())
    - docs/runbook.md (line 31 cites BUSINESS_RULES.md §5; lines 47, 57, 58 cite §6; CONCERNS.md tech-debt section lists all four locations)
    - .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md (D-04, D-06 area and `<canonical_refs>` block mention NOTION_PAGE_ID — every occurrence is the wrong name)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §9 (risk register documents the timezone, env-var, and §5/§6 fixes as Phase-1-scope changes)
    - BUSINESS_RULES.md (§3 is data health; §4 is table grain — these are the correct targets for the runbook refs that currently say §5 and §6)
    - CHANGELOG.md if it exists; create at repo root if missing
  </read_first>
  <action>
    Make four scoped string substitutions, all under one CHANGELOG entry dated 2026-05-25.

    1. SQL timezone fix (HEALTH-02): in `sql/04_data_health_check.sql` replace every bare `CURRENT_DATE()` with `CURRENT_DATE('America/Phoenix')` — four occurrences in the DATE_DIFF calls. In `sql/02_top_full_length_videos.sql` line 20 `DATE_DIFF(CURRENT_DATE(), DATE(m.published_at), DAY)` becomes `DATE_DIFF(CURRENT_DATE('America/Phoenix'), DATE(m.published_at), DAY)`. In `sql/03_age_controlled_performance.sql` make the same substitution everywhere bare `CURRENT_DATE()` appears. Confirm `sql/01_latest_snapshot_overview.sql` has no bare `CURRENT_DATE()` to fix (read first; do not edit if absent). Do NOT change any other line of these SQL files; do not touch `youtube_analytics` hardcoding — the recipe (Plan 03) handles dataset substitution at runtime per D-02 + RESEARCH §9.

    2. Runbook section refs: in `docs/runbook.md`, replace `BUSINESS_RULES.md §5` with `BUSINESS_RULES.md §3` (line ~31) and `BUSINESS_RULES.md §6` with `BUSINESS_RULES.md §4` (lines ~47, ~57, ~58). After the edit, no `§5` or `§6` reference to BUSINESS_RULES.md remains anywhere in `docs/runbook.md`.

    3. CONTEXT.md env-var reconciliation (per RESEARCH §10 Q1, recommendation: keep `NOTION_REPORT_PAGE_ID`): in `.planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md`, replace every occurrence of `NOTION_PAGE_ID` with `NOTION_REPORT_PAGE_ID` (about 4–5 occurrences in `<decisions>` and `<canonical_refs>`). The body prose is the source of conflict with `.env.example` line 28; we are aligning to the shipped name to minimize churn.

    4. CHANGELOG entry: prepend a single dated section to `CHANGELOG.md` (create the file with a top-level `# Changelog` heading if missing). Use this exact form (substituting today's date):

    ```
    ## 2026-05-25 — Phase 1 scaffold fixups

    - Timezone: replaced bare `CURRENT_DATE()` with `CURRENT_DATE('America/Phoenix')` in sql/02, sql/03, sql/04. Aligns staleness and `days_since_published` with the rule in BUSINESS_RULES.md §3.
    - Runbook: corrected `BUSINESS_RULES.md §5`→§3 and `§6`→§4 cross-references in docs/runbook.md.
    - CONTEXT alignment: 01-CONTEXT.md prose updated from NOTION_PAGE_ID to NOTION_REPORT_PAGE_ID to match shipped .env.example and docs/runbook.md.
    ```

    Do not touch `.env.example` (already correct). Do not edit `CLAUDE.md` or `BUSINESS_RULES.md` in this task.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        cd /Users/kylechalmers/Development/channel-patterns-analyzer
        echo "Check 1: zero bare CURRENT_DATE() in sql/04, sql/02, sql/03 (excluding comments)"
        ! grep -nE "^[^-]*CURRENT_DATE\(\)" sql/04_data_health_check.sql sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql
        echo "Check 2: every CURRENT_DATE call in sql/02 sql/03 sql/04 uses America/Phoenix (in non-comment lines)"
        test "$(grep -vE "^\s*--" sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql sql/04_data_health_check.sql | grep -cE "CURRENT_DATE\(\x27America/Phoenix\x27\)")" -ge 6
        echo "Check 3: docs/runbook.md no longer references BUSINESS_RULES.md §5 or §6"
        ! grep -E "BUSINESS_RULES\.md §[56]" docs/runbook.md
        echo "Check 4: 01-CONTEXT.md no longer says NOTION_PAGE_ID"
        ! grep -E "(^|[^_])NOTION_PAGE_ID" .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md
        echo "Check 5: CHANGELOG.md exists and dated entry is present"
        grep -q "2026-05-25 — Phase 1 scaffold fixups" CHANGELOG.md
      '
    </automated>
  </verify>
  <done>
    All five automated checks pass. No bare `CURRENT_DATE()` remains in sql/02/03/04. docs/runbook.md has no §5/§6 references. 01-CONTEXT.md uses only `NOTION_REPORT_PAGE_ID`. CHANGELOG.md has the dated fixup entry.
  </done>
  <acceptance_criteria>
    - `grep -nE "CURRENT_DATE\(\)" sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql sql/04_data_health_check.sql` returns empty (zero exit-1, no matching lines outside comments).
    - `grep -c "CURRENT_DATE('America/Phoenix')" sql/04_data_health_check.sql` returns exactly 4 (one per UNION ALL block).
    - `grep -E "BUSINESS_RULES\.md §[56]" docs/runbook.md` returns no matches.
    - `grep -c "NOTION_REPORT_PAGE_ID" .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md` returns ≥ 3; `grep -E "(^|[^_])NOTION_PAGE_ID" .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md` returns no matches.
    - `grep -q "2026-05-25 — Phase 1 scaffold fixups" CHANGELOG.md` exits 0.
    - `sql/02_top_full_length_videos.sql` byte-for-byte unchanged outside line 20.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore to commit the write-notion-report Skill and extend runs/README.md schema</name>
  <files>.gitignore, runs/README.md</files>
  <read_first>
    - .gitignore (line 39 has `.claude/skills/`; line 38 has the explanatory comment that this is for "Live-generated artifacts")
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §8 (documents the dual-negation pattern `!dir/` + `!dir/**` and the gotcha if either line is missing)
    - runs/README.md (current `summary.json` schema example, lines 32–57)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §6 (documents the additive `transport` and `notion_url` fields)
  </read_first>
  <action>
    Two edits in two files.

    1. `.gitignore` — keep line 39 (`.claude/skills/`) unchanged. Immediately AFTER line 39, insert these four lines:

    ```
    # Phase 1: ship the write-notion-report Skill so the repo has a working Notion
    # writer out of the box. Other Skills under .claude/skills/ remain live-built
    # during the video and stay ignored.
    !.claude/skills/write-notion-report/
    !.claude/skills/write-notion-report/**
    ```

    Both negation lines are required — the directory negation lets git descend, the `**` negation un-ignores the contents (per RESEARCH §8). Do NOT remove or reorder the existing `.claude/skills/` line — the negation only works when the broad ignore precedes it.

    2. `runs/README.md` — in the `summary.json schema` JSON block (between the existing `"bq_dataset"` line and the `"snapshot_dates"` line), add a `"transport"` field with example value `"bq_cli"`. After the existing `"notion_page_id"` line, add a `"notion_url"` field with a sample value `"https://www.notion.so/Weekly-report-2026-05-24-<shortid>"`. Update the prose immediately below the JSON block: add one sentence noting that `transport` is `"bq_cli"` or `"bq_mcp"` depending on the runtime probe (per D-03), and that `notion_url` is set when `notion_write_ok` is true. Keep the rest of the file intact.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        cd /Users/kylechalmers/Development/channel-patterns-analyzer
        echo "Check 1: gitignore has both negation lines after the broad ignore"
        grep -nE "^\.claude/skills/$" .gitignore
        grep -nE "^!\.claude/skills/write-notion-report/$" .gitignore
        grep -nE "^!\.claude/skills/write-notion-report/\*\*$" .gitignore
        echo "Check 2: a hypothetical Skill path is NOT ignored"
        mkdir -p .claude/skills/write-notion-report
        : > .claude/skills/write-notion-report/.probe
        if git check-ignore -q .claude/skills/write-notion-report/.probe; then echo "FAIL: still ignored"; rm .claude/skills/write-notion-report/.probe; exit 1; fi
        echo "Check 3: a different Skill path IS still ignored"
        mkdir -p .claude/skills/some-other-skill
        : > .claude/skills/some-other-skill/.probe
        git check-ignore -q .claude/skills/some-other-skill/.probe
        rm .claude/skills/write-notion-report/.probe .claude/skills/some-other-skill/.probe
        rmdir .claude/skills/some-other-skill 2>/dev/null || true
        echo "Check 4: summary.json schema now has transport and notion_url"
        grep -q "\"transport\":" runs/README.md
        grep -q "\"notion_url\":" runs/README.md
      '
    </automated>
  </verify>
  <done>
    `.gitignore` un-ignores only `.claude/skills/write-notion-report/**` while keeping the rest of `.claude/skills/` ignored. `runs/README.md` documents the additive `transport` and `notion_url` fields. Both `git check-ignore` probes return the expected values.
  </done>
  <acceptance_criteria>
    - `git check-ignore -q .claude/skills/write-notion-report/SKILL.md` exits 1 (not ignored).
    - `git check-ignore -q .claude/skills/foo-other/SKILL.md` exits 0 (still ignored).
    - `grep -c "transport" runs/README.md` ≥ 2 (one in JSON, one in prose).
    - `grep -c "notion_url" runs/README.md` ≥ 2.
    - The pre-existing `.gitignore` line `.claude/skills/` is still present and appears BEFORE the new negation lines.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Probe BigQuery MCP and Notion MCP argument shapes; record findings</name>
  <files>.planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md</files>
  <read_first>
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §2 (Notion MCP REST shape and parent-page constraint)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §4 (BigQuery MCP `execute_sql_readonly` expected shape and Wave-0 probe instructions)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md "Wave 0 Gaps" subsection (lists the four probes)
    - .env.example (confirm `NOTION_REPORT_PAGE_ID` is the env var name)
  </read_first>
  <what-built>
    Two probes that pin down argument shapes Plans 02 and 03 will hardcode. The result is a single file at `.planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md` capturing:

    1. **BigQuery MCP probe** — call `mcp__claude_ai_Google_Cloud_BigQuery__execute_sql_readonly` with a one-row test query and record (a) the exact argument key names the tool accepts (likely `sql` and `project_id`, possibly `query` instead), (b) the response shape (rows as list of dicts? as nested arrays? what does the field key look like?), (c) the error message if `project_id` is omitted, and (d) whether the tool requires `--dialect=standard` or accepts standard SQL by default.

    2. **Notion preflight probe** — call `mcp__claude_ai_Notion__notion-fetch` against the `NOTION_REPORT_PAGE_ID` from `.env` (the operator's actual value, not a sample) and record (a) whether the page is reachable, (b) the parent page title returned, (c) whether dashed or undashed UUID form is required, and (d) the integration permissions visible from the response.

    Probe template — the operator runs these two MCP calls in the active session and pastes outputs into `01-01-PROBE-NOTES.md` using the template Claude generates. Claude proposes the file with placeholders pre-filled and asks the operator to run the two MCP calls and paste raw outputs. Operator approves or rejects the captured shapes.

    The query for the BigQuery probe is: `SELECT 1 AS one, 'hello' AS label`. Use this exact text. If the call fails with a credentials error, record the failure verbatim — the recipe in Plan 03 must match against this error pattern.

    For the Notion probe: read the operator's `.env` (do NOT echo the value into the probe-notes file — just confirm it is set and use it for the fetch). If `NOTION_REPORT_PAGE_ID` is not set, the probe fails for Phase 1 and the checkpoint blocks until the operator sets it.

    File layout for `01-01-PROBE-NOTES.md`:

    ```
    # Wave-0 probe notes (Phase 1)

    Probed: <date>
    Probed by: <operator>

    ## BigQuery MCP — execute_sql_readonly

    - Tool name: mcp__claude_ai_Google_Cloud_BigQuery__execute_sql_readonly
    - Test query: SELECT 1 AS one, 'hello' AS label
    - Argument key for SQL: <"sql" or "query" or other>
    - Argument key for project: <"project_id" or "project" or other; record "n/a" if not required>
    - Response shape (paste the raw response):
      ```
      <paste here>
      ```
    - Error if project omitted: <verbatim error or "not tested">

    ## Notion MCP — notion-fetch against NOTION_REPORT_PAGE_ID

    - Tool name: mcp__claude_ai_Notion__notion-fetch
    - Env var: NOTION_REPORT_PAGE_ID (value not recorded for hygiene)
    - Fetch result (paste the parent page object metadata, not the full body):
      ```
      <paste here>
      ```
    - Parent page title: <title from response>
    - UUID form accepted: <"dashed" / "undashed" / "both">
    - Notes / failure modes encountered:
      <free text>
    ```
  </what-built>
  <how-to-verify>
    1. Open this Claude Code session (or any session that has both MCPs loaded — `mcp__claude_ai_Google_Cloud_BigQuery__execute_sql_readonly` and `mcp__claude_ai_Notion__notion-fetch`).
    2. Confirm `.env` has `NOTION_REPORT_PAGE_ID` set to a real Notion page UUID. If it is empty, set it before continuing.
    3. Run the BigQuery MCP probe: ask Claude to call `mcp__claude_ai_Google_Cloud_BigQuery__execute_sql_readonly` with the test query `SELECT 1 AS one, 'hello' AS label` (Claude will need the argument keys — start with `{"sql": "...", "project_id": "<your BQ_PROJECT>"}` and observe whether the call succeeds or returns a validation error pointing at a different key name).
    4. Run the Notion probe: ask Claude to call `mcp__claude_ai_Notion__notion-fetch` with `{"id": "<NOTION_REPORT_PAGE_ID value>"}` (or whatever argument key the MCP wrapper exposes — adjust per the error message if the key is different).
    5. Confirm `01-01-PROBE-NOTES.md` exists at the path above, that every "<…>" placeholder has been filled, and that the paste blocks contain real outputs.
    6. Spot-check: the BigQuery response contains a row with `one=1` and `label="hello"`. The Notion response contains the parent page title (it should match Kyle's channel-patterns page).
  </how-to-verify>
  <resume-signal>Reply "approved" once `01-01-PROBE-NOTES.md` is populated with real outputs from both probes, or describe what failed (e.g., "Notion fetch returned 404 — page ID is wrong") so the recipe in Plan 03 can be adjusted before it lands.</resume-signal>
  <acceptance_criteria>
    - `.planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md` exists.
    - The file contains the literal string `execute_sql_readonly`.
    - The file contains the literal string `notion-fetch`.
    - The file contains at least one real response paste block (non-empty, non-placeholder) for each probe.
    - No environment variable values (Notion page UUIDs, BQ project IDs) are written into the file in clear text — only metadata like "set / not set" or "title returned".
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo file system → committed git history | The probe-notes file must not contain raw env var values; the gitignore must keep `.env` excluded |
| operator's MCP-loaded session → external MCP servers (Notion, BigQuery) | Probe calls cross to a Google Cloud project and a Notion workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | 01-01-PROBE-NOTES.md (committed) | mitigate | Task 3 acceptance criteria explicitly forbid writing env var values into the file; only metadata is recorded |
| T-01-02 | Tampering | sql/ scaffold edits | mitigate | Task 1 acceptance criteria check that no SQL semantics changed beyond the timezone substitution; byte-for-byte stability check on the rest of sql/02 line 20's surroundings |
| T-01-03 | Tampering | .gitignore (broad-skill ignore vs Skill commit) | mitigate | Task 2 verifies both branches (write-notion-report is NOT ignored; arbitrary other Skill IS still ignored) using live `git check-ignore` probes |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installs no packages — see RESEARCH "Package Legitimacy Audit". No checkpoint needed. |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -rE "CURRENT_DATE\(\)" sql/` returns no non-comment matches (timezone fix is consistent across the scaffold).
2. `grep -rE "BUSINESS_RULES\.md §[56]" docs/` returns no matches.
3. `git check-ignore -q .claude/skills/write-notion-report/foo.md` exits 1.
4. `grep -q "transport" runs/README.md && grep -q "notion_url" runs/README.md` exits 0.
5. `.planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md` exists and references both `execute_sql_readonly` and `notion-fetch`.
6. `CHANGELOG.md` has the dated 2026-05-25 entry covering the timezone, runbook §5/§6, and env-var alignment fixes.
</verification>

<success_criteria>
- Every scaffold defect identified in RESEARCH §9 as Phase-1-scope is resolved before Plans 02 and 03 build on top of it.
- Probe notes pin down the two MCP shapes Plans 02 and 03 will encode.
- The single CHANGELOG entry captures the work in one place — future operators can see the timezone, runbook, and env-var alignment landed together on the same date.
- No code in `sql/`, `docs/`, `runs/`, or `.planning/phases/01-…/01-CONTEXT.md` references the wrong env var name (`NOTION_PAGE_ID`) or the wrong BUSINESS_RULES section (§5/§6) by the end of this plan.
</success_criteria>

<output>
Create `.planning/phases/01-first-notion-report-end-to-end/01-01-SUMMARY.md` when done, capturing: which SQL files were edited, what the probe revealed (key names for both MCPs), and any deviation between the documented probe shape (RESEARCH §2/§4) and the observed shape.
</output>
