---
phase: 01-first-notion-report-end-to-end
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - docs/runbook.md
  - reports/
  - runs/
  - CHANGELOG.md
autonomous: false
requirements:
  - ERR-02
  - BQ-03
  - NOTION-06
  - HEALTH-03
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03

user_setup:
  - service: notion
    why: "End-to-end live run publishes a real child page on the channel-patterns Notion page"
    env_vars:
      - name: NOTION_REPORT_PAGE_ID
        source: "Notion page URL: notion.so/<workspace>/<page-id> — use the page-id portion"
    dashboard_config:
      - task: "Confirm the Notion integration has access to the channel-patterns parent page"
        location: "Notion → Settings → Integrations → channel-patterns workspace"

must_haves:
  truths:
    - "A real end-to-end /run-analyzer execution against live BigQuery + live Notion succeeds; a new child page appears under the channel-patterns parent page titled `Weekly report — {run_date}`."
    - "The published page leads with a Data Health section that names the snapshot_date of every table and flags daily_video_analytics + daily_traffic_sources (both 89 days stale as of 2026-05-24)."
    - "Local artifacts (`reports/{run_date}.md`, `runs/{run_date}/summary.json`, `runs/{run_date}/queries/*.json`) match the Notion page content."
    - "When NOTION_REPORT_PAGE_ID is temporarily unset and the recipe re-runs, the run still produces `reports/{run_date}.md` and `runs/{run_date}/summary.json` with `notion_write_ok: false` and a populated `errors[]` (PERSIST-03 + ERR-02)."
    - "docs/runbook.md has exactly one named section per Phase-1-scope failure mode actually surfaced by the recipe — BigQuery auth, missing table, Notion write failed; the env-var-missing fallback is also covered."
    - "Operator messages from the recipe map 1:1 to docs/runbook.md section names (a grep of the recipe's `docs/runbook.md §'…'` strings finds matching `## …` headings in the runbook)."
  artifacts:
    - path: "reports/{run_date}.md"
      provides: "Live-generated weekly report from the first real /run-analyzer execution"
      contains: "Data Health"
    - path: "runs/{run_date}/summary.json"
      provides: "Audit-trail metadata for the first real run, including notion_url and snapshot_dates"
      contains: "notion_write_ok"
    - path: "CHANGELOG.md"
      provides: "Dated entry confirming Phase 1 end-to-end success and any new failure modes encountered"
      contains: "Phase 1 first end-to-end run"
  key_links:
    - from: "operator message in .claude/commands/run-analyzer.md"
      to: "docs/runbook.md section heading"
      via: "string match between recipe's runbook quote and runbook's `## ` heading"
      pattern: "## (BigQuery auth failure|Required table is missing or empty|Notion write failed|A required table is stale)"
---

<objective>
Run the full `/run-analyzer` end-to-end against live BigQuery + live Notion, capture the artifacts, and align `docs/runbook.md` so every operator-message section name in the recipe (Plan 03) maps to a real runbook heading. This is the Phase-1 success gate.

Purpose: Phases 1's success criteria require an actual Notion child page within 60 seconds of run trigger AND a survivable failure path that writes local artifacts when Notion is unreachable. Both can only be verified by running the recipe against live infrastructure.

Output: A real `reports/{run_date}.md` + `runs/{run_date}/summary.json` from the first end-to-end run, a runbook with all Phase-1 failure-mode sections present and named the same way the recipe references them, and a CHANGELOG entry recording the milestone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md
@.planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md
@.planning/phases/01-first-notion-report-end-to-end/01-01-PROBE-NOTES.md
@.claude/commands/run-analyzer.md
@.claude/skills/write-notion-report/SKILL.md
@CLAUDE.md
@BUSINESS_RULES.md
@docs/runbook.md
@runs/README.md
@reports/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align docs/runbook.md section names with operator-message strings the recipe emits</name>
  <files>docs/runbook.md, CHANGELOG.md</files>
  <read_first>
    - .claude/commands/run-analyzer.md (the three operator-message templates from Plan 03 Step 8 — extract the exact runbook section names they cite: "BigQuery auth failure", "Required table is missing or empty", "Notion write failed", plus any other section names referenced from Steps 2–6)
    - .claude/skills/write-notion-report/SKILL.md (the error categories the Skill returns: env_missing, parent_not_found, permission_denied, transport_error, unknown — each should resolve to a runbook section)
    - docs/runbook.md (current sections: "BigQuery auth failure", "A required table is stale", "Required table is missing or empty", "BigQuery schema drift", "Notion write failed", "Report says something the operator believes is wrong" — five Phase-1-relevant headings already exist)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §7 (failure-mode catalog → runbook section mapping)
    - CHANGELOG.md (read current top entry to know where to prepend the new one)
  </read_first>
  <action>
    Two scoped edits.

    1. **Runbook alignment.** Open `docs/runbook.md` and compare its `## ` headings against every `docs/runbook.md § '…'` quote in `.claude/commands/run-analyzer.md`. The recipe's three operator messages cite three runbook sections. Confirm each cited name appears in the runbook verbatim. If any cited name does NOT appear (e.g., the recipe says "Notion write failed" and the runbook says "Notion publishing error"), align the runbook heading to match the recipe — do not change the recipe (the recipe is the contract; the runbook follows it).

    Additionally, add ONE new section if missing: `## Required environment variable is missing`. Trigger: the recipe's Step 0 preflight stops with `category: "env_missing"` when `NOTION_REPORT_PAGE_ID`, `BQ_PROJECT`, or `BQ_DATASET` is unset. Recovery steps:
       - For `NOTION_REPORT_PAGE_ID`: get the page ID from the Notion page URL (`notion.so/<workspace>/<page-id>`) and add it to `.env` (gitignored).
       - For `BQ_PROJECT`: run `gcloud config set project <id>` or set the env var in `.env`.
       - For `BQ_DATASET`: defaults to `youtube_analytics`; set explicitly if your dataset is named differently.
       Note: this is the `env_missing` category surfaced in `summary.json.errors[]`. Phase 3's ERR-01 work expands runbook coverage further; Phase 1 just adds this one section to close the loop on what the recipe actually surfaces.

    Also check `## A required table is stale`. The current body says "per BUSINESS_RULES.md §5" — Plan 01 should already have fixed this to `§3`. If a §5 reference remains, fix it; if `§3` is correct, leave it alone.

    Do NOT add sections for `category: "skill_unavailable"`, `category: "report_dict_invalid"`, or `category: "no_bigquery_transport"` — these are Phase-1 transient categories and Phase 3 (ERR-01) covers exhaustive runbook section creation. Phase 1's scope is the minimum runbook coverage the recipe's three operator-message templates actually need.

    2. **CHANGELOG entry.** Prepend a single dated entry to `CHANGELOG.md` (top of file, immediately under the `# Changelog` heading, above the Plan 01 fixup entry from 2026-05-25):

    ```
    ## <today's date YYYY-MM-DD> — Phase 1 first end-to-end run

    - Ran /run-analyzer end-to-end against live BigQuery + live Notion. New child page created on the channel-patterns parent. Local artifacts at reports/{run_date}.md and runs/{run_date}/.
    - docs/runbook.md gained one new section: "Required environment variable is missing". Other runbook headings already matched the recipe's operator-message strings.
    - Any new failure modes encountered during the live run are added below; if no new modes surfaced, this entry says so explicitly.
    ```

    Fill in `{run_date}` with the actual date once Task 2 has run. If Task 2 surfaces a new failure mode not in RESEARCH §7's catalog, add a one-line entry under this CHANGELOG section naming it (and add the corresponding runbook section). This is the Phase-1-scope subset of ERR-03 — full ERR-03 lands in Phase 3.
  </action>
  <verify>
    <automated>
      bash -c '
        set -e
        cd /Users/kylechalmers/Development/channel-patterns-analyzer
        echo "Check 1: every runbook section the recipe cites exists in docs/runbook.md"
        for section in "BigQuery auth failure" "Required table is missing or empty" "Notion write failed"; do
          grep -qF "## $section" docs/runbook.md || { echo "MISSING SECTION: $section"; exit 1; }
        done
        echo "Check 2: env-var-missing section added"
        grep -qF "## Required environment variable is missing" docs/runbook.md
        echo "Check 3: no remaining §5 or §6 cross-references"
        ! grep -E "BUSINESS_RULES\.md §[56]" docs/runbook.md
        echo "Check 4: CHANGELOG has the Phase-1-end-to-end entry"
        grep -qF "Phase 1 first end-to-end run" CHANGELOG.md
      '
    </automated>
  </verify>
  <done>
    Every section name the recipe cites is present as a `## ` heading in `docs/runbook.md`. The new "Required environment variable is missing" section covers the recipe's Step 0 preflight failure. No BUSINESS_RULES §5/§6 references remain. CHANGELOG has the Phase-1 milestone entry.
  </done>
  <acceptance_criteria>
    - `grep -F "## BigQuery auth failure" docs/runbook.md` exits 0.
    - `grep -F "## Required table is missing or empty" docs/runbook.md` exits 0.
    - `grep -F "## Notion write failed" docs/runbook.md` exits 0.
    - `grep -F "## Required environment variable is missing" docs/runbook.md` exits 0.
    - `grep -E "BUSINESS_RULES\.md §[56]" docs/runbook.md` returns no matches.
    - `grep -F "Phase 1 first end-to-end run" CHANGELOG.md` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run /run-analyzer end-to-end and verify all five Phase-1 success criteria</name>
  <files>reports/{run_date}.md, runs/{run_date}/summary.json, runs/{run_date}/queries/*.json, runs/{run_date}/report.md</files>
  <read_first>
    - .claude/commands/run-analyzer.md (the recipe being executed)
    - .claude/skills/write-notion-report/SKILL.md (the Skill the recipe invokes in Step 6)
    - .env (real values — confirm BQ_PROJECT, BQ_DATASET, NOTION_REPORT_PAGE_ID all set)
    - .planning/ROADMAP.md (Phase 1 "Success Criteria" — five items the operator confirms below)
    - .planning/phases/01-first-notion-report-end-to-end/01-RESEARCH.md §"Phase Requirements → Verification Map" (concrete checks per requirement)
  </read_first>
  <what-built>
    The Phase-1 success gate: one happy-path end-to-end run, then one forced-failure run to prove ERR-02 + PERSIST-03.

    **Phase 2a — Happy-path run.**
    1. Confirm `gcloud auth login` is fresh (RESEARCH §4 observed a stale-auth case during research; refresh before the run). Run `bq query --use_legacy_sql=false 'SELECT 1 AS one'` once interactively to confirm auth works.
    2. Confirm `.env` has `BQ_PROJECT`, `BQ_DATASET` (or default), and `NOTION_REPORT_PAGE_ID` all set.
    3. From the Claude Code terminal in this project, run `/run-analyzer`. No arguments.
    4. Within 60 seconds, expect:
       - A new child page under the channel-patterns Notion parent page, titled `Weekly report — {today's Phoenix date}`.
       - `reports/{run_date}.md` exists in the repo.
       - `runs/{run_date}/summary.json` exists with `notion_write_ok: true` and a populated `notion_url`.
       - `runs/{run_date}/queries/data_health.json` and `runs/{run_date}/queries/top_full_length_videos.json` both exist with non-empty JSON.
    5. Open the Notion page and confirm:
       - Title contains the run date.
       - Data Health section appears FIRST and names all four tables.
       - Both stale tables (`daily_video_analytics` and `daily_traffic_sources`, ~89 days stale per STATE.md note 2026-05-24) appear as ⚠ callout blocks.
       - The "What is working" section names one full-length video, with a confidence label.
       - The other three sections (`What is not working`, `Patterns worth watching`, `Open questions`) are present with labeled placeholders — none silently omitted.

    **Phase 2b — Forced-failure run (PERSIST-03 + ERR-02 verification).**
    6. Temporarily comment out `NOTION_REPORT_PAGE_ID` in `.env` (prepend `#`).
    7. Run `/run-analyzer` again. Expect:
       - The recipe STOPS at Step 0 preflight (per Plan 03 Task 1 Step 0 behavior).
       - `runs/{run_date_2}/summary.json` is written with `errors: [{ category: "env_missing", message: "NOTION_REPORT_PAGE_ID not set", step: "preflight" }]`.
       - `reports/{run_date_2}.md` is NOT written (per Step 0 behavior — we stop before drafting). But `summary.json` exists.
       - The operator message names `docs/runbook.md § 'Required environment variable is missing'` (the section Task 1 just added).
    8. Restore `NOTION_REPORT_PAGE_ID`.

    **Phase 2c — Stale-data integration test (HEALTH-03 verification).**
    9. The happy-path run from Phase 2a IS the stale-data integration test if `daily_video_analytics` remains 89 days stale. Confirm by reading `runs/{run_date}/summary.json.stale_tables` — should have ≥ 1 entry. If the upstream pipeline has caught up between roadmap creation (2026-05-24) and Phase-1 execution and no tables are stale, log this in the SUMMARY as an artifact: the Phase-1 happy path no longer doubles as the HEALTH-03 integration test, and a separate dry-run with a forced stale-table simulation would be needed (out of Phase 1 scope; flag for Phase 2 follow-up).
  </what-built>
  <how-to-verify>
    Run the checks above in order. After the happy-path run completes, paste the operator-message lines from the recipe (Step 8 output) into the Task 2 SUMMARY along with:

    - The Notion page URL produced.
    - The contents of `runs/{run_date}/summary.json` (sanitize the `bq_project` value if private).
    - The first 30 lines of `reports/{run_date}.md` (to confirm structure).

    For the forced-failure run, paste:

    - The operator-message line.
    - The `errors[]` field from `runs/{run_date_2}/summary.json`.

    Operator confirms each of the five ROADMAP success criteria with a yes/no:
    1. New child page appeared under channel-patterns within 60 seconds — yes/no.
    2. Page leads with Data Health section naming snapshot_date per table and flagging stale tables — yes/no.
    3. `runs/{run_date}/queries/*.json` and `runs/{run_date}/summary.json` written per `runs/README.md` schema — yes/no.
    4. Forced-failure run produced `summary.json` with `errors[]` populated; operator saw a runbook-pointed message — yes/no.
    5. Reading `.claude/skills/write-notion-report/SKILL.md` frontmatter in isolation, the trigger was unambiguous — yes/no.

    If any check is "no", describe what failed and which plan needs revision before declaring Phase 1 complete.
  </how-to-verify>
  <resume-signal>Reply "approved" once both runs are done and all five success criteria are confirmed, OR describe what failed (e.g., "Notion title was wrong" — Plan 02 Task 2 needs revision; "summary.json missing notion_url" — Plan 03 Task 2 needs revision).</resume-signal>
  <acceptance_criteria>
    - `test -f reports/{run_date}.md` exits 0 after the happy-path run (filename uses the actual Phoenix date of the run).
    - `test -f runs/{run_date}/summary.json` exits 0.
    - `test -f runs/{run_date}/queries/data_health.json` and `test -f runs/{run_date}/queries/top_full_length_videos.json` both exit 0.
    - `jq -e '.notion_write_ok == true' runs/{run_date}/summary.json` exits 0.
    - `jq -e '.notion_url | startswith("https://www.notion.so/")' runs/{run_date}/summary.json` exits 0.
    - `jq -e '.snapshot_dates | length == 4' runs/{run_date}/summary.json` exits 0.
    - For the forced-failure run: `jq -e '.errors[0].category == "env_missing"' runs/{run_date_2}/summary.json` exits 0.
    - For the forced-failure run: `test -f reports/{run_date_2}.md` exits 1 (per Step-0 behavior, no report when we stop at preflight).
    - The published Notion page (verified visually) contains the literal string "Data Health" before any other section heading.
    - The published Notion page contains at least one ⚠ callout block (the stale-table flag), unless no tables are stale at run time (operator flags this in SUMMARY).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live BigQuery + live Notion | This task crosses the trust boundary into real external services for the first time in the phase |
| committed reports/{run_date}.md | The report content is published publicly via Notion AND committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | committed reports/{run_date}.md may include sensitive data | mitigate | The data source is YouTube public-channel analytics (view counts, titles); no PII. Operator visually confirms the published page before approving Task 2. |
| T-01-04-02 | Information Disclosure | summary.json contains bq_project value | accept | Per RESEARCH §"Security Domain", project IDs are non-secret. README §3 already documents the same value publicly. |
| T-01-04-03 | Tampering | runbook section names drift from recipe operator messages over time | mitigate | Task 1 acceptance criteria establish a 1:1 grep check between recipe operator-message section names and runbook headings. Future plans (Phase 3 ERR-01) extend this check. |
| T-01-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
After both tasks complete:

1. `docs/runbook.md` has the four required Phase-1 section headings (BigQuery auth failure, Required table is missing or empty, Notion write failed, Required environment variable is missing).
2. CHANGELOG records the milestone.
3. Live `/run-analyzer` execution produces a Notion child page within 60 seconds.
4. `reports/{run_date}.md` and `runs/{run_date}/summary.json` exist after the happy-path run.
5. Forced-failure run produces `summary.json` with `errors[]` populated and no `reports/{run_date_2}.md`.
6. Operator confirms all five ROADMAP Phase-1 success criteria.
</verification>

<success_criteria>
- One real /run-analyzer run published a Notion child page on the channel-patterns parent and produced matching local artifacts (success criteria 1, 2, 3 from ROADMAP).
- The forced-failure run proved the PERSIST-03 + ERR-02 contracts: local artifacts survive even when the Notion write is blocked (success criterion 4).
- The Skill's frontmatter-only invocation test passes (success criterion 5).
- The runbook now has a named section for every failure mode the recipe surfaces.
</success_criteria>

<output>
Create `.planning/phases/01-first-notion-report-end-to-end/01-04-SUMMARY.md` when done. Capture:
- The actual run_date(s) used.
- The Notion page URL from the happy-path run.
- The `summary.json.errors` from the forced-failure run.
- Which (if any) of the five ROADMAP success criteria required follow-up work; if all five passed first-try, say so.
- Any new failure modes encountered that warrant a Phase-3 runbook section (out of Phase 1 scope but should be queued for ERR-01).
</output>
