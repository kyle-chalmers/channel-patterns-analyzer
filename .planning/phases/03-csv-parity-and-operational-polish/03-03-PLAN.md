---
phase: 03-csv-parity-and-operational-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - .claude/commands/run-analyzer.md
  - docs/schedule.md
  - .env.example
  - CHANGELOG.md
autonomous: true
gap_closure: false
requirements:
  - CSV-01
  - CSV-02
  - SCHED-01
must_haves:
  truths:
    - "Operator with DATA_SOURCE=csv set runs the analyzer and produces `reports/{date}.md` with all six sections and a top-of-report `data source: csv (sample fixtures, not live)` line"
    - "Operator with DATA_SOURCE unset (or =bigquery) sees the recipe's BigQuery branches unchanged"
    - "Operator following docs/schedule.md top-to-bottom can create a working cloud routine on first try (numbered steps with concrete claude.com field names, no references to service-account keys)"
    - "Operator pasting `.claude/commands/run-analyzer.md` into a cloud routine's Instructions box does not see any `.planning/` references that a cloud-only context cannot resolve"
    - "docs/schedule.md's Run-now checklist contains exactly four verifications (Plan 03-04 wires items b/c/d to specific runbook section anchors created there)"
    - ".env.example contains only the four env vars the analyzer actually reads (DATA_SOURCE, BQ_PROJECT, BQ_DATASET, NOTION_REPORT_PAGE_ID)"
  artifacts:
    - path: ".claude/commands/run-analyzer.md"
      provides: "Recipe with three-branch transport probe (csv | bq_cli | bq_mcp) and zero .planning/ references"
      contains: "DATA_SOURCE=csv"
      contains_2: "TRANSPORT=csv"
    - path: "docs/schedule.md"
      provides: "Numbered cloud-routine walkthrough with concrete claude.com field names + Run-now checklist (D-03, D-04)"
      contains: "channel-patterns-analyzer-weekly"
      contains_2: "Run-now checklist"
      contains_3: "BigQuery web connector"
    - path: ".env.example"
      provides: "Minimal env template (4 vars only: DATA_SOURCE, BQ_PROJECT, BQ_DATASET, NOTION_REPORT_PAGE_ID)"
    - path: "CHANGELOG.md"
      provides: "Phase 3 Plan 03-03 entry under today's date"
  key_links:
    - from: ".claude/commands/run-analyzer.md Step 1"
      to: "scripts/csv_query.py + scripts/csv_fallback_loader.py"
      via: "DATA_SOURCE=csv short-circuit before bq probe; regenerate fixtures via csv_fallback_loader.py, then dispatch each SQL step to csv_query.py"
      pattern: "scripts/csv_query\\.py"
    - from: "docs/schedule.md cloud walkthrough"
      to: ".claude/commands/run-analyzer.md"
      via: "step instructs operator to paste recipe contents into the Instructions box"
      pattern: "\\.claude/commands/run-analyzer\\.md"
    - from: "docs/schedule.md Run-now checklist"
      to: "docs/runbook.md"
      via: "each failure-case bullet links to a runbook section; Plan 03-04 Task 5 will rewrite items b/c/d to cite the exact new runbook anchors Plan 03-04 Task 1 creates"
      pattern: "docs/runbook\\.md"
---

## Plan Structure Rationale

This plan owns **Slice 2 of MVP Phase 3 (Cloud routine launchable from claude.com)** plus the recipe-side completion of Slice 1 (CSV mode end-to-end runnable). It depends on Plans 03-01 (loader takes `--snapshot-date`) and 03-02 (`scripts/csv_query.py` exists and is shape-correct).

Four logically-coupled edits ride together because they share the same source-of-truth file or the same operator-facing surface:

1. **CSV branch in the recipe** (CSV-01, CSV-02): adds the `DATA_SOURCE=csv` short-circuit in Step 1 and the CSV dispatch arm in Steps 2, 3, 5.
2. **Recipe cloud-portability cleanup**: dereferences six `.planning/` prose references the cloud-routine context cannot meaningfully resolve, per `03-RESEARCH.md` § "Cloud-Portability Audit".
3. **Cloud routine setup walkthrough** (SCHED-01, D-04): extends `docs/schedule.md` with the numbered field-by-field walkthrough and the D-03 Run-now checklist, and fixes the schedule.md:22 portability bug (the "Service account key" cell, which conflicts with D-01). Note: SCHED-02 verification is owned by Plan 03-04 Task 6 (cloud smoke-test checkpoint); this plan provides the doc surface SCHED-02's verification runs against.
4. **`.env.example` cleanup** (tech-debt fold-in): removes four unused vars (`YOUTUBE_CHANNEL_ID`, `ANALYSIS_LOOKBACK_DAYS`, `MIN_VIDEO_AGE_DAYS`, `SCHEDULE_TIMEZONE`) per `03-RESEARCH.md` § "Tech Debt Scope Decision" recommendation. Rides naturally with SCHED-01 because the cloud-routine env-vars section in the walkthrough enumerates the real vars.

**Wave 2 placement.** Depends on 03-01 (loader can pin `--snapshot-date`, which the recipe's "if CSV, regenerate first" step references) and 03-02 (`scripts/csv_query.py` must exist and be shape-correct because the recipe's CSV dispatch invokes it).

**MVP slice check.** After this plan ships:
- BigQuery-mode end-to-end runnable (unchanged from Phase 2).
- CSV-mode end-to-end runnable (new: Slice 1 complete).
- Cloud routine launchable from claude.com using the walkthrough (new: Slice 2 complete).
- Runbook deepening (Slice 3) and SCHED-02 cloud smoke-test verification (live "Run now" + recorded result) are the only remaining work; Plan 03-04 owns both.

## Worktree Setup

**If this plan is executed in a git worktree** (the default for `/gsd-execute-phase` parallel waves), the worktree may have been created off a base commit older than current `main`. Phase 1 and Phase 2 fixes shipped to `main` after Phase 3 was planned, so a stale worktree base will diverge from the production state the plan was written against. This plan modifies `.claude/commands/run-analyzer.md` whose line numbers have already shifted since RESEARCH.md was written; a stale base would compound that drift.

Before starting Task 1, run:

```bash
git fetch origin main
git rebase origin/main
```

If the rebase reports conflicts in files this plan owns (`files_modified` in frontmatter), stop and report the conflict; do not silently resolve. If conflicts are in files this plan does NOT touch, resolve in favor of `origin/main` and continue.

After rebase, re-run the Task 1 pre-check (`grep -cnF '.planning/' .claude/commands/run-analyzer.md` should return 6) before applying any edits — the count may have shifted if a Phase 1/2 fix touched the recipe.

If you are working directly on `main` (no worktree), skip this section.

## Phase Goal (User Story)

**As a** solo channel operator, **I want to** (a) demo the analyzer in CSV-mode without BigQuery auth and (b) launch the weekly cloud routine from claude.com following a doc top-to-bottom, **so that** new viewers can fork the repo and replicate the workflow without me coaching them, and so that I can stop running the analyzer manually on Mondays.

<objective>
Extend `.claude/commands/run-analyzer.md` with three changes (CSV branch in the transport probe + CSV dispatch in Steps 2/3/5 + dereference of six `.planning/` prose references), rewrite the cloud-routine portion of `docs/schedule.md` with a numbered field-by-field walkthrough plus a Run-now smoke-test checklist, and clean up `.env.example` to the four env vars the analyzer actually reads.

Purpose: per **CSV-01**, **CSV-02** the recipe owns the source-side branch. Per **SCHED-01**, **D-04** the schedule doc owns the operator-facing setup steps with concrete claude.com field names so an operator launches on the first try. Per **D-01** the cloud auth path is the BigQuery web connector (no service-account keys); per **D-02** the recipe is the single source of truth and the operator copy-pastes it verbatim into the Instructions box; per **D-03** a four-item Run-now checklist gates trust in the Monday schedule. SCHED-02 (Skill works identically local + cloud) is verified by Plan 03-04 Task 6's live smoke test against the cloud routine this plan stands up.

Output: a recipe that branches three ways at Step 1, a schedule.md that walks an operator through the cloud routine creation form by field name, a minimal `.env.example`, and a CHANGELOG entry documenting every edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@BUSINESS_RULES.md
@.planning/phases/03-csv-parity-and-operational-polish/03-CONTEXT.md
@.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md
@.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md
@.claude/commands/run-analyzer.md
@docs/schedule.md
@.env.example
@CHANGELOG.md
@scripts/csv_query.py
@scripts/csv_fallback_loader.py
@.planning/phases/03-csv-parity-and-operational-polish/03-01-PLAN.md
@.planning/phases/03-csv-parity-and-operational-polish/03-02-PLAN.md
</context>

<executor-notes>
**Task count: 4 (at warning threshold per planner heuristics).** Acceptable here because:
- Tasks 2-4 are short-prose / config-file edits (low context cost each).
- The four edits share a single operator-facing surface (cloud-routine setup) and naturally serialize.

**Context budget heads-up:** Task 1 is the heaviest (recipe edits across 5 groups; new prose ~60-80 lines). Land Task 1 first; if context approaches 50% before Task 4, `/clear` and resume — Tasks 2-4 are independent of Task 1's working memory once Task 1's output is on disk. Expected total: 40-50% context if executed in one pass.

**Wave-2 dependencies confirmed:** Plan 03-01 (loader) and Plan 03-02 (csv_query.py) must be ON DISK before this plan starts. Task 1 Group A references `scripts/csv_fallback_loader.py` and Task 1 Group B references `scripts/csv_query.py`; both must exist.
</executor-notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add CSV branch + dereference .planning refs in run-analyzer.md</name>
  <files>.claude/commands/run-analyzer.md</files>

  <read_first>
    - `.claude/commands/run-analyzer.md` (in full, currently 391 lines including frontmatter — the file grew from the RESEARCH.md-era 348 lines after Phase 2 plans extended it). **Important:** line numbers in this plan's read_first and action sections refer to the CURRENT state of the file. The six `.planning/` prose references are now at lines 82, 170, 181, 250, and 252 (twice). The Step 1 transport probe is at lines 24-38. Use `grep -n '^## Step ' .claude/commands/run-analyzer.md` to confirm the current step boundaries before editing.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "`.claude/commands/run-analyzer.md` (MOD ...)" for the canonical insertion shape: the CSV branch bullet to add before the existing `bq_cli` probe, the CSV dispatch bullet to add to the invocation-shapes block, the dispatch-call-site language for Steps 2/3/5, and the explicit before/after replacement text for each of the six `.planning/` references.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Cloud-Portability Audit" for the per-line rationale on each of the six dereferences (NOTE: RESEARCH.md cites old line numbers from the 348-line file; the prose anchors below are based on the current 391-line file), AND § "CSV Output JSON Shape Contract" for the contract the CSV dispatch must satisfy, AND § "Open Questions for Planner" Q3 for the locked decision: "short-circuit; if DATA_SOURCE=csv then TRANSPORT=csv; else probe bq/MCP".
    - `scripts/csv_query.py` (created by Plan 03-02) for the exact CLI shape (`python scripts/csv_query.py <query_name>` with `query_name` in `{data_health, top_full_length_videos, eligible_video_count}`) the recipe will invoke.
    - `scripts/csv_fallback_loader.py` (post-Plan-03-01) for the preflight invocation shape (`python scripts/csv_fallback_loader.py` with optional `--snapshot-date`).
    - `BUSINESS_RULES.md` § "Data health expectations" for the staleness rule the CSV branch must respect (CSV-mode regenerates fixtures every run, so `days_stale = 0` is the expected state unless `--snapshot-date` was passed).
  </read_first>

  <action>
    **Robustness pre-check (run FIRST, before any edits):**
    ```bash
    grep -cnF '.planning/' .claude/commands/run-analyzer.md
    ```
    This MUST return 6 (the six `.planning/` references Group E will dereference). If it returns any other number, STOP and surface the discrepancy in your SUMMARY — the file has drifted from the planning assumption and the dereferencing strategy needs reconciliation before edits proceed. If it returns 6, proceed.

    Apply five edit groups to `.claude/commands/run-analyzer.md`.

    Group A: Step 1 transport probe (current recipe lines 24-38). Insert a new first probe-branch bullet ABOVE the existing `Try \`command -v bq\` via Bash...` bullet. The new bullet:

    > If `DATA_SOURCE=csv` (read from the operator's `.env` in Step 0), set `TRANSPORT=csv` and SKIP the BigQuery probe. Before proceeding to Step 2, regenerate `sample_data/*.csv` for today's Phoenix snapshot by running `python scripts/csv_fallback_loader.py` (no arguments; the loader anchors on Phoenix tz by default after Plan 03-01). The fixtures are stable-seeded (`Random(42)`, `Random(43)`, `Random(44)`) so regenerating is cheap and deterministic. If the operator wants to exercise the stale-data branch, they pre-run `python scripts/csv_fallback_loader.py --snapshot-date YYYY-MM-DD` themselves before invoking the recipe; the recipe does not auto-stale.

    The existing `bq_cli` and `bq_mcp` bullets follow unchanged. Per RESEARCH.md Open Question 3 (locked recommendation: short-circuit), the CSV branch MUST be a short-circuit, not a parallel probe. If the operator sets `DATA_SOURCE=csv`, the recipe does NOT try `command -v bq` (it would falsely succeed on Kyle's machine and confuse the audit trail in `summary.json.transport`).

    Group B: Invocation shapes block (current recipe lines 32-36). Add a new bullet to the bulleted list following "Invocation shapes (use verbatim):". The new bullet:

    > `csv`: `python scripts/csv_query.py <query_name>` where `<query_name>` is one of `data_health`, `top_full_length_videos`, `eligible_video_count`. The helper reads `sample_data/*.csv`, applies the same filters/joins as the corresponding `sql/*.sql` file (or, for `eligible_video_count`, the inline SQL in Step 5), and emits a JSON array to stdout matching `bq --format=json query`'s shape (top-level array, all values as JSON strings, column names matching the SQL aliases). No `--project_id` or dataset arg; CSV mode is project-agnostic. Capture stdout to the same `runs/{run_date}/queries/<query_name>.json` path the BigQuery branches write to. No `.stderr` sidecar (Python writes tracebacks to stderr only on failure; success path is clean stdout). On non-zero exit, capture stderr inline into the error block per the failure-routing rules at the bottom of each step.

    Group C: Dispatch call sites. Three locations (Steps 2, 3, 5) say "Dispatch ... to `$TRANSPORT`". Each gets one short sentence added (do NOT re-spec the dispatch shape; that lives in Step 1's invocation-shapes block). Locate each by string-search for the unique step heading (`^## Step 2`, `^## Step 3`, `^## Step 5`) — do not rely on line numbers.

    Step 2 (after the current "Dispatch the rewritten SQL to `$TRANSPORT`..." sentence): "When `$TRANSPORT=csv`, the dispatch is `python scripts/csv_query.py data_health` (the in-memory SQL substitution from the prior paragraph is a no-op; the CSV helper does not consume SQL). Output captured to the same `runs/{run_date}/queries/data_health.json` path."

    Step 3 (after the analogous "Dispatch to `$TRANSPORT`" sentence in Step 3): "When `$TRANSPORT=csv`, the dispatch is `python scripts/csv_query.py top_full_length_videos`. Same output path."

    Step 5 (after the analogous "Dispatch via `$TRANSPORT`" sentence in Step 5): "When `$TRANSPORT=csv`, the dispatch is `python scripts/csv_query.py eligible_video_count`. Same output path. The inline SQL composed earlier in this step is informative for the BigQuery branch only; the CSV helper has the same logic hand-coded."

    Group D: Top-of-report `data source` annotation in Step 6. Per CONTEXT.md § "Claude's Discretion" the CSV-mode report carries one extra top line. Add one new sub-bullet under Step 6 § "1. Rules to apply before writing prose" (or as a peer sub-section, executor choice as long as it lands BEFORE the six-section structure heading):

    > **CSV-mode annotation.** When `$TRANSPORT=csv`, the report's first line (above the `## Data Health` heading) reads exactly `data source: csv (sample fixtures, not live)`. This is the only structural difference between a CSV-mode report and a BigQuery-mode report. Do not add a similar line for BigQuery mode (the absence of this line signals live data).

    Group E: Dereference six `.planning/` prose references. **Navigate by string anchor, not line number** — the file has shifted from the 348-line RESEARCH.md-era version (and may shift again before this task runs). For each replacement below, the `Anchor` line is a short distinctive prose chunk the executor uses with `grep -n '<chunk>' .claude/commands/run-analyzer.md` to locate the line; the `BEFORE` and `AFTER` blocks are the exact strings to replace.

    **Replacement 1.** Anchor: `ANALYSIS-05 and D-08 from`
    - BEFORE: `This step implements ANALYSIS-05 and D-08 from \`.planning/phases/02-honest-analyst-depth/02-CONTEXT.md\`. Run AFTER Step 3 (top-videos pull) and BEFORE the draft step.`
    - AFTER: `This step implements ANALYSIS-05 (read prior reports for confidence calibration) and the prior-report citation rule (do not cite prior reports in prose; banned phrases listed in Step 6 below). Run AFTER Step 3 (top-videos pull) and BEFORE the draft step.`

    **Replacement 2.** Anchor: `D-07 inline-parenthetical format`
    - BEFORE: `Compose the markdown report per \`CLAUDE.md § "Report structure"\`. This step implements REPORT-01 (six-section structure), REPORT-02 (numbers + age + confidence in plain sight), and the D-07 inline-parenthetical format (per \`.planning/phases/02-honest-analyst-depth/02-CONTEXT.md\`).`
    - AFTER: `Compose the markdown report per \`CLAUDE.md § "Report structure"\`. This step implements REPORT-01 (six-section structure), REPORT-02 (numbers + age + confidence in plain sight), and the D-07 inline-parenthetical format.`
    - (Action: delete the parenthetical `(per \`.planning/...\`)` and the surrounding space; leave the sentence intact.)

    **Replacement 3.** Anchor: `- Apply D-08 from`
    - BEFORE: `- Apply D-08 from \`.planning/phases/02-honest-analyst-depth/02-CONTEXT.md\`. The prior reports read in Step 4 are calibration memory, not reader memory. Do not cite them in prose. Cross-week framing per D-09 is allowed only when self-contained (e.g., \`"For the third consecutive week, X has held."\`). Banned phrases: \`"as we said last week"\`, \`"as noted previously"\`, \`"the prior report"\`, \`"this continues the trend we observed"\`.`
    - AFTER: `- Apply the prior-report citation rule. The prior reports read in Step 4 are calibration memory, not reader memory. Do not cite them in prose. Cross-week framing per D-09 is allowed only when self-contained (e.g., \`"For the third consecutive week, X has held."\`). Banned phrases: \`"as we said last week"\`, \`"as noted previously"\`, \`"the prior report"\`, \`"this continues the trend we observed"\`.`

    **Replacement 4.** Anchor: `D-01 Layer 2 from`
    - BEFORE: `This step implements D-01 Layer 2 from \`.planning/phases/02-honest-analyst-depth/02-CONTEXT.md\`: Step 6 made the rules explicit at draft time (Layer 1); this step verifies the rules were actually followed.`
    - AFTER: `This step is the self-audit gate (Layer 2 of the voice-and-structure verification): Step 6 made the rules explicit at draft time (Layer 1); this step verifies the rules were actually followed.`
    - (Action: replace only the first sentence of the paragraph; everything after the colon "Step 6 made the rules explicit..." stays intact in both BEFORE and AFTER. The rest of the long paragraph about silent voice violations etc. is unchanged.)

    **Replacement 5.** Anchor: `cited in \`.planning/phases/02-honest-analyst-depth/02-RESEARCH.md`
    - BEFORE: ` (cited in \`.planning/phases/02-honest-analyst-depth/02-RESEARCH.md\` § "Common Pitfalls" 3)`
    - AFTER: ` (cited in Anthropic's Skill best practices)`
    - (Action: rewrite the parenthetical to attribute the source without the `.planning/` path. The sentence containing this parenthetical otherwise stays intact.)

    **Replacement 6.** Anchor: `mirrors \`CLAUDE.md\` and \`02-CONTEXT.md\` rules 1:1`
    - BEFORE: `mirrors \`CLAUDE.md\` and \`02-CONTEXT.md\` rules 1:1`
    - AFTER: `mirrors \`CLAUDE.md\` rules 1:1`

    After all six are applied, `grep -nF '.planning/' .claude/commands/run-analyzer.md` returns ZERO matches. The pre-edit count was 6 (verified in the robustness pre-check above); the post-edit count MUST be 0.

    Per `CLAUDE.md` § "Voice": all new prose added to the recipe avoids em dashes, banned vocab (`leverage`, `robust`, `delve`, `seamless`, `navigate`, `underscore`, `showcase`, `tapestry`, `realm`, `multifaceted`, `transformative`, `testament to`), and formulaic openers/closers.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; ! grep -nF '.planning/' .claude/commands/run-analyzer.md; grep -c 'DATA_SOURCE=csv' .claude/commands/run-analyzer.md; grep -c 'TRANSPORT=csv' .claude/commands/run-analyzer.md; grep -c 'python scripts/csv_query.py' .claude/commands/run-analyzer.md; grep -c 'python scripts/csv_fallback_loader.py' .claude/commands/run-analyzer.md; grep -F 'data source: csv (sample fixtures, not live)' .claude/commands/run-analyzer.md; grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted' .claude/commands/run-analyzer.md</automated>
  </verify>

  <acceptance_criteria>
    - **Starting state check (run FIRST):** `grep -cnF '.planning/' .claude/commands/run-analyzer.md` returns 6 BEFORE any edits. If not 6, task halts and the executor reports the discrepancy. The pre-edit count of 6 is asserted; the post-edit count of 0 is the target.
    - `grep -nF '.planning/' .claude/commands/run-analyzer.md` returns ZERO matches AFTER editing (all six dereferences applied).
    - `grep -c 'DATA_SOURCE=csv' .claude/commands/run-analyzer.md` returns at least 1.
    - `grep -c 'TRANSPORT=csv' .claude/commands/run-analyzer.md` returns at least 1.
    - `grep -c 'python scripts/csv_query.py' .claude/commands/run-analyzer.md` returns at least 4 (one in Step 1 invocation shapes + one each in Steps 2/3/5 dispatch additions).
    - `grep -c 'python scripts/csv_fallback_loader.py' .claude/commands/run-analyzer.md` returns at least 2 (regenerate-before-Step-2 reference + the `--snapshot-date` operator note).
    - `grep -F 'data source: csv (sample fixtures, not live)' .claude/commands/run-analyzer.md` returns one match in Step 6.
    - The Step 1 CSV branch appears BEFORE the `command -v bq` branch in source order (`grep -n 'DATA_SOURCE=csv\|command -v bq' .claude/commands/run-analyzer.md | head -2` shows `DATA_SOURCE=csv` first).
    - The existing `bq_cli` and `bq_mcp` invocation-shape bullets are unmodified (`grep -c 'printf.*bq.*format=json' .claude/commands/run-analyzer.md` returns at least 1).
    - The SIMULATE_STALE block (still in Step 2) is unchanged — verify by `grep -c 'SIMULATE_STALE' .claude/commands/run-analyzer.md` returns at least 5 (was 5 pre-edit; should still be 5 post-edit).
    - No em dashes (U+2014) in the new content (`grep -nF "—" .claude/commands/run-analyzer.md` does not match any line inside the new CSV-branch or new dispatch additions).
    - No banned vocabulary in new content (`grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted' .claude/commands/run-analyzer.md` returns ZERO matches OR only matches text that was already in the file pre-edit; reviewer confirms by comparing to git diff).
  </acceptance_criteria>

  <done>
    Recipe has a three-branch transport probe with CSV short-circuit, all four dispatch call sites recognize the CSV transport, the top-of-report annotation lands when CSV mode is active, and all six `.planning/` prose references are dereferenced. Group E replacements were navigated by string anchor, not line number, so the executor was robust to the file having shifted between RESEARCH.md (348 lines) and the current state (391 lines).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite cloud routine section of docs/schedule.md (walkthrough + Run-now checklist + portability bug fix)</name>
  <files>docs/schedule.md</files>

  <read_first>
    - `docs/schedule.md` (in full, 48 lines) to identify: the existing "What the routine does" section (lines 5-14, keep), the "Local vs. cloud" comparison table (lines 16-25, the BigQuery auth cell on line 22 says "Service account key in routine env vars" which is the portability bug to fix per CONTEXT.md D-01 and PATTERNS.md), the "Re-running manually" section (lines 29-39, keep), the "Changing the schedule" section (lines 41-43, keep), the "Reference" section (lines 45-48, keep).
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "`docs/schedule.md` (MOD ...)" for the exact target structure: keep existing sections, fix the table cell, ADD a "Cloud routine setup walkthrough" section between "Local vs. cloud" and "Re-running manually", ADD a "Run-now checklist" section after the walkthrough. The PATTERNS section gives the full target text for both new sections.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Cloud Routine Setup Walkthrough Requirements" for the live-verified claude.com UI field names (Routine name, Instructions, Repositories, Environment, Trigger, Connectors, Permissions, Environment variables) and the exact values to recommend (`channel-patterns-analyzer-weekly`, Weekly/Monday/9:00 AM/America/Phoenix, the three env vars).
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Run-Now Checklist Scoping" for the four checklist items in their final form (per Option 2 recommendation: a, b, c, d items as listed there) AND § "Cloud-Portability Audit" subsection "Assumed tools the cloud routine will or will not have" for the "no bq CLI, no gcloud CLI" facts that justify the BigQuery-web-connector-only walkthrough.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-CONTEXT.md` § "Cloud `/schedule` Routine" D-01 through D-04 (locked decisions to honor).
    - `docs/runbook.md` (current section anchors) so the Run-now checklist links can target real anchors: § "BigQuery auth failure", § "A required table is stale", § "Required table is missing or empty", § "BigQuery schema drift", § "Notion write failed", § "Required environment variable is missing". Note: Plan 03-04 Task 5 will REWRITE the Run-now checklist items (b)/(c)/(d) anchors after Plan 03-04 Task 1 creates the new runbook sections. For THIS plan, write the checklist with the most-relevant currently-existing anchors (e.g., "Notion write failed" for item a, the generic "see runbook" pointer for items b/c/d). Plan 03-04 Task 5 closes the cross-doc link loop.
    - `CLAUDE.md` § "Voice" for prose constraints (no em dashes, no banned vocab, no formulaic openers).
  </read_first>

  <action>
    Apply four edit groups to `docs/schedule.md`.

    Group A: Fix the portability bug in the "Local vs. cloud" table (line 22). Change the cell text "Service account key in routine env vars" to "BigQuery web connector (authorized once in your Anthropic account)". The rest of the table is unchanged. This aligns with D-01 (cloud routine uses the BigQuery web connector, no service-account keys anywhere).

    Group B: Insert a new section titled `## Cloud routine setup walkthrough` between the existing "Local vs. cloud" section and the "Re-running manually" section. The section's contents:

    Opening paragraph (one sentence): "Run once when first wiring the routine. After that, edits use the same form (see Changing the schedule below)."

    Numbered list, nine items, following the PATTERNS.md § "New Cloud routine setup walkthrough section" template verbatim, with these exact field names and values:

    1. Authorize the BigQuery and Notion connectors once. In your Anthropic account at https://claude.com, go to Settings, Connectors. Verify both BigQuery (Google Cloud) and Notion are connected. The cloud routine has no `bq` or `gcloud` CLI installed; the BigQuery connector is the only data path.
    2. Open the Routines page. claude.com, Settings, Routines, New Routine.
    3. Routine name: `channel-patterns-analyzer-weekly`.
    4. Instructions. Paste the full contents of `.claude/commands/run-analyzer.md` into the Instructions box. The recipe is self-contained; do not edit it after pasting (edits drift from the repo source; see `docs/maintenance.md`).
    5. Repositories. Select `channel-patterns-analyzer`. Required: Step 4 of the recipe reads prior reports from the cloned repo.
    6. Trigger. Select Schedule, Weekly, Monday, 9:00 AM, America/Phoenix.
    7. Connectors. Verify BigQuery (Google Cloud) and Notion appear under Connectors. Remove any others (Slack, Linear, etc.) that are not needed.
    8. Environment variables. Open the environment's settings (the cloud icon below the Instructions box, then the settings icon, then Environment variables) and add three:
       - `NOTION_REPORT_PAGE_ID=<from your local .env>`
       - `BQ_PROJECT=<your GCP project id>`
       - `BQ_DATASET=youtube_analytics`
    9. Click Create.

    Use placeholder values only (per the security-threat-model directive at the phase level: no secrets in committed files; the walkthrough uses generic values like `<your GCP project id>`). The reader fills in their own values.

    Group C: Insert a new section titled `## Run-now checklist` directly after the walkthrough section. Opening paragraph: "Click Run now on the routine's detail page. Verify within about 3 minutes:"

    Then a four-item checkbox list (the exact form recommended in RESEARCH.md § "Run-Now Checklist Scoping" Option 2):

    - [ ] (a) A new child page appears under the channel-patterns parent in Notion within 60 seconds, titled `Weekly report, {today's Phoenix date}`. If not, see `docs/runbook.md` § "Notion write failed".
    - [ ] (b) The Notion page renders all six sections: Data Health, Headline, What is working, What is not working, Patterns worth watching, Open questions. Spot-check one finding for a `(label, n=N)` parenthetical; Notion should render it as plain text (not stripped, not linkified).
    - [ ] (c) The Anthropic UI shows the routine run as completed (green status). Note: green means the session exited without an infrastructure error, not that the analysis succeeded. Open the run transcript and confirm no `category: ...` error lines appear.
    - [ ] (d) After the cloud routine's PR merges (or you `git pull` the `claude/...` branch), `runs/{date}/summary.json` exists with `notion_write_ok: true` and an empty `errors: []` array.

    Closing sentence: "If any check fails, the linked runbook section names the recovery. New failure modes (BigQuery connector not authorized, Notion connector not authorized, routine env var missing, routine timed out, Anthropic UI shows error before recipe runs) get their own runbook sections in Plan 03-04, and Plan 03-04 Task 5 rewrites items (b)/(c)/(d) above to cite those exact anchors."

    Group D: Keep all other sections of `docs/schedule.md` unchanged (the existing "What the routine does", "Re-running manually", "Changing the schedule", and "Reference" sections).

    Per `CLAUDE.md` § "Voice": no em dashes; use commas, periods, or parentheses. No banned vocab. No formulaic openers (avoid phrases like "Great!", "In conclusion,", "Overall,"). The walkthrough uses imperative voice ("Open the Routines page", "Select Schedule") which is correct for operator instructions.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; ! grep -nF 'Service account key' docs/schedule.md; grep -F 'BigQuery web connector' docs/schedule.md; grep -F 'channel-patterns-analyzer-weekly' docs/schedule.md; grep -F 'Cloud routine setup walkthrough' docs/schedule.md; grep -F 'Run-now checklist' docs/schedule.md; grep -cE '^- \[ \] \([a-d]\)' docs/schedule.md; grep -F 'America/Phoenix' docs/schedule.md; grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted' docs/schedule.md; grep -nF '—' docs/schedule.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nF 'Service account key' docs/schedule.md` returns ZERO matches (the portability bug is fixed).
    - `grep -F 'BigQuery web connector (authorized once in your Anthropic account)' docs/schedule.md` returns 1 match (the fix landed in the Local-vs-cloud table cell).
    - `grep -F 'channel-patterns-analyzer-weekly' docs/schedule.md` returns 1 match (in the routine name walkthrough step).
    - `grep -F 'Cloud routine setup walkthrough' docs/schedule.md` returns 1 match (the new H2 heading).
    - `grep -F 'Run-now checklist' docs/schedule.md` returns 1 match (the new H2 heading).
    - `grep -cE '^- \[ \] \([a-d]\)' docs/schedule.md` returns exactly 4 (the four checkbox items, each prefixed with (a)/(b)/(c)/(d)).
    - The numbered walkthrough has exactly 9 items (`grep -cE '^[0-9]+\. ' docs/schedule.md` returns 9, or executor verifies by reading the section).
    - `grep -F '`.claude/commands/run-analyzer.md`' docs/schedule.md` returns at least 1 (walkthrough step 4 instructs operator to paste this file).
    - `grep -F 'America/Phoenix' docs/schedule.md` returns at least 1 (in the trigger step).
    - `grep -F 'NOTION_REPORT_PAGE_ID' docs/schedule.md` returns at least 1 (in env vars step).
    - `grep -F 'BQ_PROJECT' docs/schedule.md` returns at least 1.
    - `grep -F 'BQ_DATASET' docs/schedule.md` returns at least 1.
    - Existing "What the routine does", "Re-running manually", "Changing the schedule", "Reference" sections are still present (`grep -cE '^## (What the routine does|Re-running manually|Changing the schedule|Reference)' docs/schedule.md` returns 4).
    - `grep -nF '—' docs/schedule.md` returns ZERO matches (no em dashes in the file at all; the pre-edit file had none and the new content adds none).
    - `grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted|transformative|testament to' docs/schedule.md` returns ZERO matches.
    - File length grows from 48 lines to roughly 80-110 lines (the walkthrough adds ~25-35 lines, the checklist adds ~8-12 lines).
  </acceptance_criteria>

  <done>
    `docs/schedule.md` carries a numbered cloud-routine walkthrough with concrete claude.com field names, a four-item Run-now checklist, and the fixed Local-vs-cloud table cell. Existing sections preserved. No em dashes, no banned vocab.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Clean up .env.example to the four env vars the analyzer actually reads</name>
  <files>.env.example</files>

  <read_first>
    - `.env.example` (in full, 39 lines) to confirm the current sections (Data source mode, Google Cloud / BigQuery, YouTube channel identity, Notion, Analysis configuration) and the four vars to remove (`YOUTUBE_CHANNEL_ID` at line 23, `ANALYSIS_LOOKBACK_DAYS` at line 32, `MIN_VIDEO_AGE_DAYS` at line 35, `SCHEDULE_TIMEZONE` at line 39).
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "`.env.example` (MOD ...)" for the target final shape (four vars, three sections, boxed-divider comment style preserved). The PATTERNS section provides the exact target file content.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Open Questions for Planner" Q6 for the locked recommendation: REMOVE all four unused vars; do not wire them through.
    - `.planning/codebase/CONCERNS.md` § "Unused environment variables" for the original tech-debt framing.
    - `.claude/commands/run-analyzer.md` Step 0 (recipe lines 14-22) to double-check which env vars the recipe ACTUALLY reads: `BQ_PROJECT`, `BQ_DATASET`, `NOTION_REPORT_PAGE_ID`, AND `DATA_SOURCE` (read in Step 1 per the Plan 03-03 Task 1 edits). Four total. No others.
  </read_first>

  <action>
    Rewrite `.env.example` to the four-var minimal form from PATTERNS.md § "`.env.example` (MOD ...)" subsection "Final shape (target)". Use this exact content:

    ```
    # Channel Patterns Analyzer, environment template
    # Copy this file to `.env` and fill in your own values.
    # `.env` is gitignored; never commit your real values.

    # ─── Data source mode ───────────────────────────────────────────
    # `bigquery` (default): analyzer queries BigQuery via the bq CLI (local) or BigQuery MCP (cloud routine)
    # `csv`              : analyzer reads from ./sample_data/*.csv (demo / no-BQ-auth path)
    DATA_SOURCE=bigquery

    # ─── Google Cloud / BigQuery ────────────────────────────────────
    # Project that holds the youtube_analytics dataset.
    # The bq CLI reads this from your active gcloud config OR from this env var
    # if you prefer to override per-project. Set with: gcloud config set project <id>
    BQ_PROJECT=your-gcp-project-id

    # Dataset name (default matches the youtube-bigquery-pipeline default).
    BQ_DATASET=youtube_analytics

    # ─── Notion ──────────────────────────────────────────────────────
    # The Notion page where the analyzer writes the weekly report.
    # Get this from the page URL: notion.so/<workspace>/<page-id>, use the page-id portion.
    NOTION_REPORT_PAGE_ID=
    ```

    The boxed-divider comment style (`# ─── Title ───`) is preserved verbatim from the pre-edit file. The four removed vars and their entire surrounding comment lines are gone. The "YouTube channel identity" and "Analysis configuration" section headings disappear entirely (their contents were the four removed vars).

    Per `CLAUDE.md` § "Voice": no em dashes in the comment prose. The "Channel Patterns Analyzer, environment template" line uses a comma instead of an em dash (the pre-edit version had an em dash; the rewrite removes it).

    Per the security-threat-model directive at the phase level: only placeholder values (`your-gcp-project-id`, empty `NOTION_REPORT_PAGE_ID=`); never commit real values.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; ! grep -nF 'YOUTUBE_CHANNEL_ID' .env.example; ! grep -nF 'ANALYSIS_LOOKBACK_DAYS' .env.example; ! grep -nF 'MIN_VIDEO_AGE_DAYS' .env.example; ! grep -nF 'SCHEDULE_TIMEZONE' .env.example; grep -cE '^(DATA_SOURCE|BQ_PROJECT|BQ_DATASET|NOTION_REPORT_PAGE_ID)=' .env.example; grep -nF '—' .env.example</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nF 'YOUTUBE_CHANNEL_ID' .env.example` returns ZERO matches.
    - `grep -nF 'ANALYSIS_LOOKBACK_DAYS' .env.example` returns ZERO matches.
    - `grep -nF 'MIN_VIDEO_AGE_DAYS' .env.example` returns ZERO matches.
    - `grep -nF 'SCHEDULE_TIMEZONE' .env.example` returns ZERO matches.
    - `grep -cE '^(DATA_SOURCE|BQ_PROJECT|BQ_DATASET|NOTION_REPORT_PAGE_ID)=' .env.example` returns exactly 4 (the four real env vars are each declared once at start of line).
    - `grep -nF '—' .env.example` returns ZERO matches (no em dashes; pre-edit file had at least one in the header comment).
    - `wc -l .env.example` reports between 18 and 26 lines (the trimmed file is much shorter than the 39-line pre-edit version).
    - Three section headings preserved: `grep -cE '^# ─── (Data source mode|Google Cloud / BigQuery|Notion)' .env.example` returns 3.
    - The recipe-checked vars match exactly: `grep -oE 'DATA_SOURCE|BQ_PROJECT|BQ_DATASET|NOTION_REPORT_PAGE_ID' .claude/commands/run-analyzer.md | sort -u | wc -l` returns 4, AND `grep -oE 'DATA_SOURCE|BQ_PROJECT|BQ_DATASET|NOTION_REPORT_PAGE_ID' .env.example | sort -u | wc -l` returns 4. The sets match.
  </acceptance_criteria>

  <done>
    `.env.example` carries only the four env vars the analyzer reads. Boxed-divider comment style preserved. No em dashes. Placeholder values only.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add CHANGELOG entry covering Plan 03-03's four edits + W-2 acceptance checks</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` (first 50 lines) to see Plans 03-01 and 03-02 entries (already appended in Wave 1) and confirm the existing H2-date pattern and per-file bullet shape.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "CHANGELOG discipline" for the entry conventions.
    - `CLAUDE.md` § "Voice" for the no-em-dashes / no-banned-vocab rules.
  </read_first>

  <action>
    Append the Plan 03-03 entries to `CHANGELOG.md`. If Plans 03-01 or 03-02 already added a section under today's Phoenix date, add the new bullets under the same H2 (one section per date). Otherwise create a new H2.

    Four bullets, one per file changed:

    - `.claude/commands/run-analyzer.md` (Plan 03-03): adds the third transport branch (`DATA_SOURCE=csv` short-circuits before the `bq` probe and regenerates `sample_data/*.csv` via `scripts/csv_fallback_loader.py` before Step 2; Steps 2/3/5 dispatch to `python scripts/csv_query.py <name>` when `$TRANSPORT=csv`; Step 6 prepends `data source: csv (sample fixtures, not live)` to the report when CSV-mode). Also dereferences six `.planning/` prose references that a cloud routine pasting the recipe verbatim could not resolve, per `03-RESEARCH.md` § "Cloud-Portability Audit". Before: recipe was BigQuery-only and had six `.planning/` cross-doc references. After: three-branch transport probe, zero `.planning/` references, recipe is fully self-contained for cloud paste. Impact on recent reports: zero (CSV mode is opt-in via `.env`; BigQuery-mode runs are byte-identical to Phase 2's behavior).
    - `docs/schedule.md` (Plan 03-03): adds a numbered cloud-routine setup walkthrough (9 steps, with concrete claude.com field names verified against `https://code.claude.com/docs/en/routines` on 2026-05-26) and a four-item Run-now checklist (D-03 smoke test). Fixes the portability bug in the Local-vs-cloud table cell: "Service account key in routine env vars" became "BigQuery web connector (authorized once in your Anthropic account)" per D-01 (cloud routine uses the BigQuery web connector, no service-account keys anywhere). Before: shallow "cloud column" with conflicting auth guidance. After: an operator can launch the routine on the first try without coaching. Impact on recent reports: zero. Forward-looking: enables the Monday 9am Phoenix cloud routine to actually launch.
    - `.env.example` (Plan 03-03): removes four unused env vars (`YOUTUBE_CHANNEL_ID`, `ANALYSIS_LOOKBACK_DAYS`, `MIN_VIDEO_AGE_DAYS`, `SCHEDULE_TIMEZONE`) and drops the now-empty "YouTube channel identity" and "Analysis configuration" section headings. Before: 39 lines, eight env vars, four of which no code in the repo reads. After: ~22 lines, four env vars (`DATA_SOURCE`, `BQ_PROJECT`, `BQ_DATASET`, `NOTION_REPORT_PAGE_ID`), matching exactly what `.claude/commands/run-analyzer.md` reads. Impact on recent reports: zero.
    - Indirect change: the recipe now invokes `scripts/csv_query.py` (created in Plan 03-02) and `scripts/csv_fallback_loader.py` (modified in Plan 03-01). The full CSV-mode chain works end-to-end after this plan ships.

    Per `CLAUDE.md` § "Voice": no em dashes, no banned vocab, no formulaic openers. Reference `BUSINESS_RULES.md` sections by title (`§ "Data health expectations"`), not number.

    **Optional W-2 end-to-end smoke check** (cheap; do it if time permits): after Task 1 and Task 3 land, dry-run the recipe in CSV mode with the analyzer agent and confirm `reports/{today}.md` has six sections and the top-of-report annotation line. The acceptance criteria below include the grep checks that verify the dry-run output.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; head -40 CHANGELOG.md | grep -c 'Plan 03-03'; head -40 CHANGELOG.md | grep -c '.claude/commands/run-analyzer.md'; head -40 CHANGELOG.md | grep -c 'docs/schedule.md'; head -40 CHANGELOG.md | grep -c '.env.example'; grep -nF '—' CHANGELOG.md | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `head -40 CHANGELOG.md` contains the phrase "Plan 03-03" at least once.
    - `head -40 CHANGELOG.md` contains the file paths `.claude/commands/run-analyzer.md`, `docs/schedule.md`, and `.env.example` each at least once.
    - `head -40 CHANGELOG.md` contains the phrase "Impact on recent reports:" for each new file bullet.
    - `head -40 CHANGELOG.md` contains the phrase "BigQuery web connector" (documenting the portability-bug fix).
    - No em dash (U+2014) in the new bullets (`grep -nF '—' CHANGELOG.md | grep -v 'preserved'` returns no hits in the new entry area; reviewer confirms against git diff).
    - No banned vocabulary in the new content.
    - If Plans 03-01 or 03-02 already added today's H2, no duplicate H2 date heading appears.
    - **W-2 fold-in (run after the CSV-mode dry-run if executed):** `grep -c '^## ' reports/{today}.md` returns 6 (six top-level sections) AND `grep -cF 'data source: csv (sample fixtures, not live)' reports/{today}.md` returns 1 (the top-of-report CSV annotation lands). If the dry-run was deferred (e.g., the agent cannot run the recipe end-to-end in this session), document the deferral in the SUMMARY; Plan 03-04 Task 6's cloud smoke test will cover the equivalent end-to-end verification for the cloud path.
  </acceptance_criteria>

  <done>
    `CHANGELOG.md` documents all four edits Plan 03-03 ships, under today's Phoenix date, with the standard before/after/impact bullet shape. The W-2 CSV-mode dry-run acceptance checks are wired (run if the agent can execute the recipe; defer with a note otherwise).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → `DATA_SOURCE` env var | Untrusted-input boundary in the strict sense; in practice operator-controlled via `.env` |
| Operator → cloud routine config | Untrusted-input boundary; the routine config is the cloud-side equivalent of `.env`. Walkthrough must use placeholder values only |
| Cloud routine UI screenshots / examples in docs/schedule.md | Information-disclosure boundary; secrets in committed docs would leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | `DATA_SOURCE` env var | mitigate | The recipe Step 1 short-circuit only triggers on the literal value `csv`; any other value (including unset, `bigquery`, typos like `csvv`) falls through to the existing `bq_cli`/`bq_mcp` probe. No code path interprets `DATA_SOURCE` as a shell command or file path |
| T-03-03-02 | Information disclosure | Secrets in `docs/schedule.md` walkthrough | mitigate | Walkthrough uses placeholder values only (`<your GCP project id>`, `<from your local .env>`, empty `NOTION_REPORT_PAGE_ID=`); no real `BQ_PROJECT` values, no real `NOTION_REPORT_PAGE_ID` values committed. Task 2 acceptance criteria require grep checks for these placeholder forms |
| T-03-03-03 | Information disclosure | Env-var name disclosure in `.env.example` | accept | Per the security-threat-model directive at the phase level: env-var NAMES (not values) are acceptable in committed files. The four real names (`DATA_SOURCE`, `BQ_PROJECT`, `BQ_DATASET`, `NOTION_REPORT_PAGE_ID`) are already public in the existing `.env.example` and in the recipe |
| T-03-03-04 | Tampering | Cloud routine env-var injection via the claude.com UI | accept | The cloud routine's environment variables are operator-controlled in the Anthropic UI; the threat surface is the same as `.env` on the operator's local machine. No new attack surface created by documenting the walkthrough |
| T-03-03-05 | Repudiation | Recipe edit drift between repo and pasted cloud routine | accept | Per D-02 the recipe is the source of truth and editing the recipe means re-pasting into the routine (CHANGELOG-worthy event). Drift detection is manual discipline; no automated reconciliation is in scope (FLOW-01 v2-deferred). The walkthrough step 4 explicitly says "do not edit it after pasting" |
| T-03-03-SC | Tampering | new package installs (npm/pip/cargo) | N/A | No new dependencies in this plan; only edits to existing docs/recipe/config files. The supply-chain threat does not apply. Verified against `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Project Constraints" |

**Threats explicitly NOT applicable to this plan:**
- **SQL injection**: N/A. No user-supplied SQL; `DATA_SOURCE=csv` controls dispatch only.
- **XSS**: N/A. No web UI.
- **Authn/authz**: N/A. No auth surface added; D-01 explicitly avoids service-account keys.
- **CSRF**: N/A. No state-changing endpoint.
- **Path traversal**: N/A. The recipe writes to hardcoded `runs/{run_date}/...` paths; no operator-controlled path enters the write. The CSV helper writes nothing; it only reads from the hardcoded `sample_data/` directory.

</threat_model>

<verification>

- Smoke-test CSV mode end-to-end: set `DATA_SOURCE=csv` in `.env`, invoke the recipe (manually or via `claude` CLI), confirm the run produces `reports/{date}.md` with all six sections, the top-of-report `data source: csv (sample fixtures, not live)` line, and `runs/{date}/queries/{data_health, top_full_length_videos, eligible_video_count}.json` files each containing a parseable JSON array of dicts with string values.
- Smoke-test BigQuery mode unchanged: unset `DATA_SOURCE` (or set `DATA_SOURCE=bigquery`) and run the recipe; confirm behavior is identical to Phase 2 (no new top-of-report line, transport is `bq_cli` or `bq_mcp` per the probe).
- `grep -nF '.planning/' .claude/commands/run-analyzer.md` returns zero matches (all six dereferences applied).
- `grep -nF 'Service account key' docs/schedule.md` returns zero matches (the portability bug is fixed).
- The four removed env vars do not appear anywhere in `.env.example`.
- Operator does a dry-run of the cloud routine walkthrough by reading docs/schedule.md top to bottom in a fresh tab of claude.com; every field name in the walkthrough matches a field name visible in the live UI (manual verification, since UI labels may rot per D-04's accepted risk). SCHED-02 (Skill works identically local + cloud) is verified end-to-end by Plan 03-04 Task 6's cloud smoke test, which lands the recorded result in `03-CLOUD-SMOKE-TEST.md`.
- `CHANGELOG.md` documents all four Plan 03-03 edits under today's Phoenix date.

</verification>

<success_criteria>

1. CSV-mode end-to-end runnable: `DATA_SOURCE=csv` + recipe invocation produces a structurally identical report (six sections, confidence labels, persistence artifacts) with the `data source: csv (sample fixtures, not live)` annotation at the top.
2. BigQuery-mode unchanged: omitting `DATA_SOURCE` or setting `DATA_SOURCE=bigquery` reproduces Phase 2 behavior byte-for-byte (other than the dereferenced `.planning/` references which are documentation-only).
3. Cloud routine launchable: operator follows `docs/schedule.md` top to bottom, completes the 9-step walkthrough, clicks "Run now", and the four-item checklist passes. (SCHED-02 verification owner is Plan 03-04 Task 6, which records the smoke-test result.)
4. No `.planning/` references in the recipe (`grep -nF '.planning/' .claude/commands/run-analyzer.md` returns zero).
5. No service-account-key references in `docs/schedule.md` (D-01 honored).
6. `.env.example` has exactly four env vars matching what the recipe reads.
7. CHANGELOG.md captures all four edits under today's date.
8. No em dashes, no banned vocabulary, no formulaic openers in any new content (CLAUDE.md voice rules applied throughout).

</success_criteria>

<output>
Create `.planning/phases/03-csv-parity-and-operational-polish/03-03-SUMMARY.md` when done, documenting: which six `.planning/` references were dereferenced (Anchor + before/after for each Replacement 1-6), the final shape of the schedule.md walkthrough (9 numbered steps, each step's field name), the four removed env vars and the four remaining ones, the W-2 CSV-mode dry-run result (executed | deferred with rationale), and a note that SCHED-02 verification belongs to Plan 03-04 Task 6.
</output>
