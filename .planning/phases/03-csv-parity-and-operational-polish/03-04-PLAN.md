---
phase: 03-csv-parity-and-operational-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - docs/runbook.md
  - docs/maintenance.md
  - docs/schedule.md
  - .claude/commands/run-analyzer.md
  - CHANGELOG.md
  - .planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md
autonomous: false
gap_closure: false
requirements:
  - ERR-01
  - ERR-03
  - SCHED-02
must_haves:
  truths:
    - "Every error category the recipe emits (env_missing, bq_auth, missing_table, empty_result, no_bigquery_transport, report_dict_invalid, skill_unavailable, transport_error, parent_not_found, permission_denied) maps to a named section in docs/runbook.md. The eleventh recipe category, env_invalid, folds into the 'Required environment variable is missing' section as a labeled sub-bullet because the recovery path is identical (operator fixes .env or cloud routine env vars); a separate section would split one recovery into two without adding signal."
    - "Every cloud-specific failure mode RESEARCH.md inventoried (BigQuery MCP connector not authorized, Notion connector not authorized, routine env var missing in cloud config, routine timed out or hung, Anthropic UI shows error before recipe runs) has its own runbook section following the Symptom / Fix / Recording template"
    - "docs/runbook.md no longer contains the 'service account key' Fix paragraph that conflicts with D-01"
    - "docs/maintenance.md no longer references BUSINESS_RULES.md by section number (§5, §6) and instead uses section titles"
    - "docs/maintenance.md em dashes (lines 8, 10, 21, 27, 49 of the pre-edit file) are replaced per CLAUDE.md § 'Voice' (W-1 fold-in)"
    - ".claude/commands/run-analyzer.md closes the failure-handling step with 'If this failure mode is not in docs/runbook.md, add it as part of the fix' so ERR-03 is anchored in the recipe itself"
    - "Operator hitting any new failure mode finds the recovery path within 60 seconds by scanning docs/runbook.md section headings"
    - "SCHED-02 verification owner: a manual-verify checkpoint task in this plan captures the cloud smoke-test result in 03-CLOUD-SMOKE-TEST.md; phase cannot ship green until that file exists with pass results for all four Run-now checklist items"
    - "docs/schedule.md Run-now checklist items (b)/(c)/(d) reference the exact runbook section anchors that Task 1 creates (W-3 fold-in: closes the cross-doc link loop)"
  artifacts:
    - path: "docs/runbook.md"
      provides: "Full failure-mode playbook covering all recipe error categories plus cloud-specific modes"
      min_sections: 12
      contains: "Symptom"
      contains_2: "Recording"
    - path: "docs/maintenance.md"
      provides: "Maintenance doc with BUSINESS_RULES.md references by section title (cross-ref rot fixed) and em dashes removed (CLAUDE.md voice rules)"
    - path: "docs/schedule.md"
      provides: "Run-now checklist updated with concrete runbook section anchors created by this plan"
    - path: ".claude/commands/run-analyzer.md"
      provides: "Recipe closing instruction anchoring ERR-03 in the recipe itself"
      contains: "If this failure mode is not in"
    - path: "CHANGELOG.md"
      provides: "Phase 3 Plan 03-04 entry documenting runbook expansion, cross-ref fixes, schedule.md anchor wiring, and the cloud smoke-test result"
    - path: ".planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md"
      provides: "Operator-filled record of the SCHED-02 cloud smoke test (Run-now checklist items a/b/c/d pass status, dates, notes)"
      contains: "SCHED-02"
  key_links:
    - from: "docs/schedule.md Run-now checklist"
      to: "docs/runbook.md new sections"
      via: "checklist item (a) links to '## Notion connector not authorized' and '## BigQuery MCP connector not authorized'; item (c) links to '## Routine run timed out or hung' and '## Anthropic UI shows error before recipe runs'; Task 5 rewrites items (b)/(c)/(d) to cite the exact runbook anchors Task 1 creates"
      pattern: "docs/runbook\\.md"
    - from: ".claude/commands/run-analyzer.md error-handling lines"
      to: "docs/runbook.md section headings"
      via: "every error category named in the recipe (env_missing, bq_auth, missing_table, empty_result, no_bigquery_transport, report_dict_invalid, skill_unavailable) appears as the suffix of a runbook H2 heading"
      pattern: "category.*runbook\\.md"
    - from: "03-CLOUD-SMOKE-TEST.md"
      to: "SCHED-02 (and D-03 Run-now checklist)"
      via: "operator runs the four Run-now checklist items from docs/schedule.md and records pass/fail in the smoke-test file; SCHED-02 verification rides on this artifact existing with pass results"
      pattern: "SCHED-02"
---

## Plan Structure Rationale

This plan owns **Slice 3 of MVP Phase 3 (Runbook coverage)** plus the **SCHED-02 verification gate**. It depends on Plan 03-03 because the Run-now checklist in `docs/schedule.md` links into specific runbook section anchors that this plan creates, AND because cloud-specific failure modes only became real after Plan 03-03 wired the cloud-routine setup walkthrough.

This is a docs-only plan in code terms. No scripts or SQL change. Two recipe-side edits: a single-sentence closing instruction in Step 11 anchoring ERR-03 (Task 3), and Run-now checklist anchor wiring in `docs/schedule.md` (Task 5). The analyzer's BigQuery-mode and CSV-mode runtime behavior is identical before and after this plan.

**`autonomous: false`** because Task 6 is a `checkpoint:human-verify` task: SCHED-02 requires Kyle to click "Run now" in the live claude.com routine and record pass/fail in `03-CLOUD-SMOKE-TEST.md`. Without that, the SCHED-02 requirement ("Skill works identically in both local and cloud contexts") has no executable verification owner, and the phase verifier has no auditable evidence that the cloud path works.

**Wave 3 placement.** Strictly depends on Plan 03-03 (the schedule.md Run-now checklist anchors that this plan creates).

**MVP slice check.** After this plan ships, the analyzer is end-to-end runnable in three modes (CSV, BigQuery local, BigQuery cloud routine) AND every failure path leads to a named runbook section AND the cloud-mode end-to-end path is verified by a recorded smoke-test result. The phase is complete; ROADMAP can transition.

**Tech-debt fold-ins** per `03-RESEARCH.md` § "Tech Debt Scope Decision":
- Fix the `BUSINESS_RULES.md §6` reference rot in `docs/maintenance.md:9` (Phase 1 Plan 01-04 fixed the runbook side; this is the remaining maintenance.md side).
- Fix the "Fix (scheduled cloud routine)" paragraph in `docs/runbook.md:23` which currently names service-account keys (conflicts with D-01).
- Strip five em dashes in `docs/maintenance.md` (lines 8, 10, 21, 27, 49 of the pre-edit file) per CLAUDE.md § "Voice" (W-1 from checker feedback).

## Worktree Setup

**If this plan is executed in a git worktree** (the default for `/gsd-execute-phase` parallel waves), the worktree may have been created off a base commit older than current `main`. Phase 1 and Phase 2 fixes shipped to `main` after Phase 3 was planned, so a stale worktree base will diverge from the production state the plan was written against. This plan modifies `docs/runbook.md` whose existing six section count is part of the acceptance criteria; a stale base could miscount.

Before starting Task 1, run:

```bash
git fetch origin main
git rebase origin/main
```

If the rebase reports conflicts in files this plan owns (`files_modified` in frontmatter), stop and report the conflict; do not silently resolve. If conflicts are in files this plan does NOT touch, resolve in favor of `origin/main` and continue.

After rebase, re-run the count check (`grep -cE '^## ' docs/runbook.md`) before adding sections, so the "+9 new sections" delta is computed against current state, not the pre-Phase-1/2 baseline.

If you are working directly on `main` (no worktree), skip this section.

## Phase Goal (User Story)

**As a** solo channel operator hitting an unfamiliar failure mode at 9:01 AM on a Monday, **I want to** scan `docs/runbook.md` for a section heading matching the error category in `summary.json` or the Anthropic UI's transcript and read a Symptom / Fix / Recording block, **so that** I recover the run without having to remember internal recipe steps or read SQL files cold.

<objective>
Expand `docs/runbook.md` from 6 sections to ~13 sections so every error category the recipe emits AND every cloud-specific failure mode `03-RESEARCH.md` inventoried has a named section with the Symptom / Fix / Recording template. Fix two pre-existing portability bugs: the "service account key" paragraph in `docs/runbook.md:23` (conflicts with D-01) and the `BUSINESS_RULES.md §6` reference in `docs/maintenance.md:9` (cross-reference rot). Strip the five em dashes in `docs/maintenance.md` (CLAUDE.md voice fold-in). Add a one-line closing instruction to the recipe's failure-handling step anchoring ERR-03 ("If this failure mode is not in docs/runbook.md, add it as part of the fix"). Update `docs/schedule.md` Run-now checklist items (b)/(c)/(d) to cite the exact runbook section anchors Task 1 creates. Execute the SCHED-02 cloud smoke test and record the result in `03-CLOUD-SMOKE-TEST.md` so the phase verifier has executable evidence that the Notion writer Skill works identically in the cloud context.

Purpose: per **ERR-01** every failure mode hit during Phase 1+2 builds AND the cloud-specific modes `03-RESEARCH.md` predicts (which Phase 3's smoke test will surface) must have a named runbook section with recovery steps. Per **ERR-03** the discipline of "newly-encountered failure modes get added to docs/runbook.md + CHANGELOG.md as part of the fix" needs to be visible from the recipe itself, not just in `docs/maintenance.md`. Per **SCHED-02** the cloud-routine path must produce an identical Notion result to the local path; without an executed smoke test that lands in a committed artifact, SCHED-02 has no owner and the phase verifier has nothing to audit.

Output: a runbook that an operator hitting any recipe error category or cloud-routine failure can scan in under 60 seconds and find the matching recovery section, a schedule.md whose Run-now checklist links resolve to real runbook anchors, and a `03-CLOUD-SMOKE-TEST.md` artifact recording the operator-executed verification of the cloud routine end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@BUSINESS_RULES.md
@.planning/phases/03-csv-parity-and-operational-polish/03-CONTEXT.md
@.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md
@.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md
@docs/runbook.md
@docs/maintenance.md
@docs/schedule.md
@.claude/commands/run-analyzer.md
@CHANGELOG.md
@.planning/codebase/CONCERNS.md
</context>

<executor-notes>
Five tasks plus one human-verify checkpoint — slightly above the standard 2-3 tasks-per-plan target. This is acceptable for a docs-only plan (most tasks are small grep-and-replace or short prose additions), but be deliberate about context: Task 1 is the heaviest (200+ new lines of runbook content) and should land first. Tasks 2/3/5 are each ~5-15 lines of edits. Task 4 is a CHANGELOG entry. Task 6 is a checkpoint, no automation. Total expected context: ~40-45% if executed in one pass; split with `/clear` between Task 1 and the rest if context creeps.
</executor-notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand docs/runbook.md with new sections + fix the service-account-key portability bug + map every recipe error category to a named section</name>
  <files>docs/runbook.md</files>

  <read_first>
    - `docs/runbook.md` (in full, 107 lines) to confirm the existing six sections ("BigQuery auth failure", "A required table is stale", "Required table is missing or empty", "BigQuery schema drift", "Notion write failed", "Required environment variable is missing", "Report says something the operator believes is wrong"), the canonical Symptom/Fix/Recording template at lines 9-25, AND the portability bug at line 23 ("It needs BigQuery credentials passed as environment variables (typically a service account key). Check the routine's environment config in the Anthropic UI and confirm the service account is still valid in GCP.") which conflicts with D-01.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "`docs/runbook.md` (MOD ...)" — gives the exact Symptom/Fix/Recording template to mirror for every new section, AND the replacement text for the line-23 portability bug, AND the explicit list of nine new sections to add.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Failure Mode Inventory from Phase 1 + 2 Commits" (lines 214-244) for the precise list of new failure modes with their triggers, AND § "Cloud Routine Setup Walkthrough Requirements" (lines 247-280) for the cloud-routine UI vocabulary the new sections should use (e.g., "the Anthropic UI shows the run as completed", "Open the run transcript", "category: ..." error lines).
    - `.claude/commands/run-analyzer.md` Step 0 (env_missing), Step 0 step 5 (env_invalid), Step 1 (no_bigquery_transport), Step 2 (bq_auth, missing_table, empty_result), Step 8 (report_dict_invalid), Step 9 (skill_unavailable, transport_error, parent_not_found, permission_denied) for the exact error categories the recipe emits. Every category needs a runbook section it can be linked to from operator messages.
    - `docs/schedule.md` post-Plan-03-03 (Run-now checklist section) for the runbook section anchors that already point to runbook sections this plan must create. Verify the checklist links match the new section headings exactly; Task 5 of this plan rewrites the checklist links to match the headings created here, so a heading-text drift between this task and Task 5 is a same-plan synchronization problem (executor's job to keep aligned).
    - `CLAUDE.md` § "Voice" for prose constraints (no em dashes, no banned vocab, no formulaic openers).
  </read_first>

  <action>
    Apply four edit groups to `docs/runbook.md`.

    Group A: Fix the portability bug at `docs/runbook.md:23`. The current paragraph reads:

    > **Fix (scheduled cloud routine).** The routine doesn't have your laptop's `gcloud` login. It needs BigQuery credentials passed as environment variables (typically a service account key). Check the routine's environment config in the Anthropic UI and confirm the service account is still valid in GCP. See `docs/schedule.md` for the cloud-vs-local distinction.

    Replace with:

    > **Fix (scheduled cloud routine).** The routine uses the BigQuery web connector authorized in your Anthropic account, not a service account key. If the connector is the issue, see § "BigQuery MCP connector not authorized" below. The routine has no `bq` or `gcloud` CLI; do not look for missing local-auth state. See `docs/schedule.md` § "Local vs. cloud" for the cloud-vs-local distinction.

    Group B: Fold `env_invalid` into the existing "Required environment variable is missing" section as a labeled sub-bullet. Open that section, and append a new paragraph (or a Fix sub-bullet) with this content:

    > **Sub-case: env var present but invalid (`env_invalid`).** Step 0 of the recipe validates `BQ_DATASET` against the BigQuery identifier regex `^[A-Za-z_][A-Za-z0-9_]*$`. If the value contains a backtick, comma, whitespace, or other non-identifier character, the recipe emits `category: env_invalid` and STOPs before any query. **Fix:** open `.env` (local) or the routine's environment-variables panel (cloud), correct the value, re-run. The recovery is identical to the missing-var case above; only the validation message differs. **Recording:** if you hit a class of invalid values that the regex doesn't catch but should, file a CHANGELOG entry naming the value and consider tightening the recipe's Step 0 validation.

    This folds `env_invalid` into the same recovery path as `env_missing` (operator edits config and re-runs) without creating a parallel section. The category-coverage audit at the end of the task verifies the category name is grep-findable in the file regardless.

    Group C: Add nine new sections to `docs/runbook.md`. Each follows the Symptom / Fix / Recording template (bold-then-period lead-ins, `## <Failure name>` H2 heading). Add them in this order, between the existing "Required environment variable is missing" section (now augmented per Group B) and the existing "Report says something the operator believes is wrong" section (which conceptually closes the runbook):

    1. `## BigQuery MCP connector not authorized` (cloud-specific). Symptom: cloud routine session has no `mcp__claude_ai_Google_Cloud_BigQuery__execute_sql_readonly` tool; recipe Step 1 falls through to the "no_bigquery_transport" error. Fix: open claude.com, Settings, Connectors, and re-authorize BigQuery (Google Cloud). Confirm the connector's permissions include access to the project named in `BQ_PROJECT`. Recording: if connector auth expired on a cadence (none currently known), note the next expiry in CHANGELOG.md.

    2. `## Notion connector not authorized` (cloud-specific; distinct from the existing "Notion write failed" section which covers local MCP failures and connector failures jointly). Symptom: Skill returns `{ok: false, category: "transport_error"}` from a cloud-routine session; recipe Step 9 surfaces NOTION-FAIL. Fix: open claude.com, Settings, Connectors, and re-authorize Notion. Confirm the integration has access to the parent page named by `NOTION_REPORT_PAGE_ID`. The `parent_not_found` (Notion target page not found) and `permission_denied` (Notion integration lacks write access to the parent) categories also route here; both are fixed by re-authorizing the connector and confirming the parent page is shared with the integration. Recording: same as the existing Notion section.

    3. `## Routine environment variable missing in cloud config` (cloud-specific; distinct from the existing "Required environment variable is missing" section which assumes local `.env`). Symptom: `summary.json.errors[].category` is `env_missing` from a cloud routine session; the routine config didn't include the var. Fix: open the routine in claude.com, then the environment's settings (cloud icon below Instructions, settings icon, Environment variables), and add the missing var. Re-run via "Run now". Recording: the four env vars the analyzer reads are listed in `.env.example`; reference it when adding to the routine.

    4. `## Routine run timed out or hung`. Symptom: Anthropic UI shows the run as not-completed after an unusually long duration; no `summary.json` if the recipe didn't reach Step 10. Fix: open the run transcript; look for hung tool calls (long BigQuery queries, network slowness). Cancel the run from the UI if needed. If the BigQuery query at Step 3 is the bottleneck, consider whether a `LIMIT` should be pushed into the SQL (currently 100-row bq default; see the Step 1 invocation-shapes note in `.claude/commands/run-analyzer.md`). Recording: if the same timeout recurs, file a CHANGELOG entry naming the recipe step and the duration.

    5. `## Anthropic UI shows error before recipe runs`. Symptom: routine session ends with infrastructure error before the recipe's first step executes; no `summary.json` at all. Fix: open the run transcript and read the bottom-of-session error. Common causes: repo clone failed (GitHub auth issue with the connected repo), network policy blocked an outbound call, Claude Code's cloud environment broken. If repo clone failed, re-check the Repositories field in the routine config. If network is the issue, the routine's Environment is set to `Default` per `docs/schedule.md` walkthrough step 4; verify the Default environment's network policy hasn't changed. Recording: log infrastructure errors in CHANGELOG with the date and the error excerpt.

    6. `## How to test the stale-data path without real stale data`. Operator note (not a failure mode). Symptom: you want to exercise the D-12 stale-table disclaimer rule but every analytics table is fresh. Fix: set `SIMULATE_STALE="daily_video_analytics:89,daily_traffic_sources:89"` (per the recipe Step 2 SIMULATE_STALE block at `.claude/commands/run-analyzer.md` lines 51-59) and re-run. The override mutates in-memory `data_health` rows; the on-disk `runs/{date}/queries/data_health.json` keeps the real values for the audit trail. Recording: `summary.json.warnings` will contain `simulate_stale_applied: ...` so the audit trail shows the data was synthetic.

    7. `## Skill input dict missing a required key` (`report_dict_invalid`). Symptom: `summary.json.errors[].category` is `report_dict_invalid`; recipe Step 8 caught a missing key before invoking the Skill. Fix: read the message in the error entry naming the missing key, open `.claude/skills/write-notion-report/SKILL.md` for the 8-key input contract, identify which Step in the recipe should have produced that key, and check whether the prior step actually wrote the value to working memory. Most likely cause: a refactor changed key names in one place but not the other. Recording: if the missing key is a schema drift between the recipe and the Skill, fix both and add a CHANGELOG entry naming the contract change.

    8. `## write-notion-report Skill not loaded in the session` (`skill_unavailable`). Symptom: `summary.json.errors[].category` is `skill_unavailable`; recipe Step 9 couldn't find the Skill. Fix: confirm `.claude/skills/write-notion-report/SKILL.md` exists in the repo and the runtime picked it up. For local sessions: restart Claude Code in the repo directory. For cloud routines: verify the Repositories field includes `channel-patterns-analyzer` so the cloned repo carries the `.claude/skills/` folder. Recording: if the Skill consistently fails to load in a particular session shape, file a CHANGELOG entry naming the session type.

    9. `## No BigQuery transport available` (`no_bigquery_transport`). Symptom: `summary.json.errors[].category` is `no_bigquery_transport`; recipe Step 1 found neither `bq` CLI nor the BigQuery MCP. Fix (local): install the `gcloud` SDK and authorize `bq` per the existing "BigQuery auth failure" section, OR ensure the BigQuery MCP connector is enabled in the local session. Fix (cloud routine): verify the BigQuery connector is authorized in your Anthropic account (see § "BigQuery MCP connector not authorized" above; the cloud routine has no `bq` CLI installed). Recording: if the local setup needs documentation, expand the README's setup section.

    The parenthetical category names in headings 2, 7, 8, 9 above (`transport_error`, `parent_not_found`, `permission_denied` rolled into heading 2; `report_dict_invalid` in heading 7; `skill_unavailable` in heading 8; `no_bigquery_transport` in heading 9) ensure the explicit category-to-section mapping below resolves with a single `grep -F`.

    Group D: Per `CLAUDE.md` § "Voice": every new section avoids em dashes, banned vocab, formulaic openers. Symptom/Fix/Recording lead-ins are bold-then-period (`**Symptom.**`, `**Fix.**`, `**Recording.**`) following the existing template. Use `**Fix (local).**` / `**Fix (cloud).**` only when the recovery differs between contexts; the cloud-specific sections above use a single `**Fix.**` since they are inherently cloud-only.

    **Explicit category-to-section coverage table.** Every recipe error category MUST be grep-findable in the final `docs/runbook.md`. Use this mapping (cite in your executor SUMMARY):

    | Category | Runbook target |
    |---|---|
    | `env_missing` | "Required environment variable is missing" (local) AND "Routine environment variable missing in cloud config" (cloud) |
    | `env_invalid` | "Required environment variable is missing" § sub-case `env_invalid` (Group B fold-in) |
    | `bq_auth` | "BigQuery auth failure" (existing) |
    | `missing_table` | "Required table is missing or empty" (existing) |
    | `empty_result` | "Required table is missing or empty" (existing) |
    | `no_bigquery_transport` | "No BigQuery transport available" (new) |
    | `report_dict_invalid` | "Skill input dict missing a required key" (new) |
    | `skill_unavailable` | "write-notion-report Skill not loaded in the session" (new) |
    | `transport_error` | "Notion connector not authorized" (new; the transport_error category in the Skill's return shape maps here per `.claude/skills/write-notion-report/SKILL.md` § "Return shape and error handling") |
    | `parent_not_found` | "Notion connector not authorized" (new; rolled into the heading 2 paragraph that names parent_not_found and permission_denied) |
    | `permission_denied` | "Notion connector not authorized" (new; same paragraph as parent_not_found) |
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; ! grep -nF 'service account key' docs/runbook.md; ! grep -nF 'service account is still valid' docs/runbook.md; grep -cE '^## ' docs/runbook.md; grep -cE '^\*\*Symptom\.\*\*' docs/runbook.md; grep -cE '^\*\*Fix\.\*\*|^\*\*Fix \(' docs/runbook.md; grep -cE '^\*\*Recording\.\*\*' docs/runbook.md; grep -F 'BigQuery MCP connector not authorized' docs/runbook.md; grep -F 'Notion connector not authorized' docs/runbook.md; grep -F 'Routine environment variable missing in cloud config' docs/runbook.md; grep -F 'Routine run timed out or hung' docs/runbook.md; grep -F 'Anthropic UI shows error before recipe runs' docs/runbook.md; grep -F 'How to test the stale-data path' docs/runbook.md; grep -F 'Skill input dict missing a required key' docs/runbook.md; grep -F 'write-notion-report Skill not loaded' docs/runbook.md; grep -F 'No BigQuery transport available' docs/runbook.md; grep -nF '—' docs/runbook.md; grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted' docs/runbook.md; missing=""; for cat in env_missing env_invalid bq_auth missing_table empty_result no_bigquery_transport report_dict_invalid skill_unavailable transport_error parent_not_found permission_denied; do grep -qiF "$cat" docs/runbook.md || missing="$missing $cat"; done; if [ -n "$missing" ]; then echo "MISSING CATEGORIES:$missing"; exit 1; else echo "ALL 11 CATEGORIES COVERED"; fi</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nF 'service account key' docs/runbook.md` returns ZERO matches (the portability bug is fixed).
    - `grep -nF 'service account is still valid' docs/runbook.md` returns ZERO matches.
    - `grep -F 'BigQuery web connector authorized in your Anthropic account' docs/runbook.md` returns 1 match (the replacement text is present).
    - `grep -cE '^## ' docs/runbook.md` returns AT LEAST 12 (6 existing top-level sections + at least 9 new ones, but executor may have inlined some as subsections of existing ones; minimum acceptable is 12 H2-level sections including all named in Group C).
    - Each of these nine section headings appears exactly once: `BigQuery MCP connector not authorized`, `Notion connector not authorized`, `Routine environment variable missing in cloud config`, `Routine run timed out or hung`, `Anthropic UI shows error before recipe runs`, `How to test the stale-data path`, `Skill input dict missing a required key`, `write-notion-report Skill not loaded`, `No BigQuery transport available`.
    - **Category coverage gate (mandatory):** for each of the 11 recipe error categories (`env_missing`, `env_invalid`, `bq_auth`, `missing_table`, `empty_result`, `no_bigquery_transport`, `report_dict_invalid`, `skill_unavailable`, `transport_error`, `parent_not_found`, `permission_denied`), `grep -qiF "$cat" docs/runbook.md` returns success. The verify-block loop above prints `ALL 11 CATEGORIES COVERED` on pass or `MISSING CATEGORIES: <list>` and exits non-zero on fail. The structural `grep -cE '^## '` check is necessary but not sufficient; this loop is the real category-coverage gate.
    - Every new failure-mode section has the three template lead-ins (`grep -cE '^\*\*Symptom\.\*\*' docs/runbook.md` is at least 12 and `grep -cE '^\*\*Recording\.\*\*' docs/runbook.md` is at least 8; the operator-note section "How to test the stale-data path" may legitimately use a different lead-in shape since it isn't a failure mode).
    - No em dashes in the file (`grep -nF '—' docs/runbook.md` returns 0).
    - No banned vocabulary (`grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted|transformative|testament to' docs/runbook.md` returns 0).
    - Existing six sections are preserved (`grep -cE '^## (BigQuery auth failure|A required table is stale|Required table is missing or empty|BigQuery schema drift|Notion write failed|Required environment variable is missing|Report says something the operator believes is wrong)' docs/runbook.md` returns 7).
    - File length grows from 107 lines to roughly 250-360 lines.
  </acceptance_criteria>

  <done>
    `docs/runbook.md` carries 12+ named sections including the nine new ones, each following the Symptom/Fix/Recording template. The service-account-key portability bug is fixed. The `env_invalid` category folds into the existing "Required environment variable is missing" section. The category-coverage loop confirms ALL 11 recipe error categories are grep-findable. Existing sections preserved. No em dashes, no banned vocab.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix BUSINESS_RULES.md section-number drift in docs/maintenance.md + strip em dashes</name>
  <files>docs/maintenance.md</files>

  <read_first>
    - `docs/maintenance.md` (in full, 51 lines) to identify the existing `BUSINESS_RULES.md §6` reference at line 9 (per CONCERNS.md "BUSINESS_RULES.md section-number drift") and the five em dashes at lines 8, 10, 21, 27, 49 of the pre-edit file (per W-1 in the plan-checker revision feedback).
    - `BUSINESS_RULES.md` (in full) to confirm the current section titles. The §6 reference in maintenance.md points at the "Table grain and join keys (data contract)" section (which is currently §4 in BUSINESS_RULES.md, not §6).
    - `.planning/phases/03-csv-parity-and-operational-polish/03-RESEARCH.md` § "Tech Debt Scope Decision" row for "BUSINESS_RULES.md section-number drift" (already mostly done in Plan 01-04 for runbook.md side; maintenance.md side is what this task closes).
    - `.planning/codebase/CONCERNS.md` § "BUSINESS_RULES.md section-number drift" for the original framing.
    - `CLAUDE.md` § "Voice" — em dashes are prohibited; replace with commas, periods, or parentheses depending on the sentence shape.
  </read_first>

  <action>
    Two edit groups.

    Group A: Cross-reference rot. Identify EVERY occurrence of `BUSINESS_RULES.md §N` (where N is a digit) in `docs/maintenance.md`. For each:
    1. Open `BUSINESS_RULES.md` and find the section the §N number was pointing at when the reference was written.
    2. Replace `BUSINESS_RULES.md §N` with `BUSINESS_RULES.md § "<section title>"` where `<section title>` is the literal title of the matching section.

    Known case at line 9: the sentence contains `BUSINESS_RULES.md §6` (twice in the same sentence). Both occurrences refer to "Table grain and join keys (data contract)" (currently §4 in BUSINESS_RULES.md). Replace both with `BUSINESS_RULES.md § "Table grain and join keys (data contract)"`.

    Also known case at line 21: `§1` refers to "Fiscal year start". Replace with `BUSINESS_RULES.md § "Fiscal year start"`.

    Any other §N reference the executor finds: map by reading BUSINESS_RULES.md and pick the closest section title. If no clear match exists, FLAG in SUMMARY rather than guessing.

    Group B: Em dashes (W-1 fold-in from plan-checker feedback). The pre-edit file has em dashes at lines 8, 10, 21, 27, 49. For each, replace the em dash with the appropriate punctuation:

    - Line 8: `Reference the dataset by \`BQ_DATASET\` (default \`youtube_analytics\`) — don't hardcode a project.` → replace ` — ` with `; ` (semicolon-space) OR break into two sentences. Pick whichever reads cleaner. Recommended: `Reference the dataset by \`BQ_DATASET\` (default \`youtube_analytics\`). Do not hardcode a project.`
    - Line 10: `The dataset is small for now — revisit if a query ever returns thousands of rows.` → `The dataset is small for now. Revisit if a query ever returns thousands of rows.`
    - Line 21: `Do **not** change the fiscal-year anchor (§1) without discussing — every prior report assumed July.` → `Do **not** change the fiscal-year anchor (per BUSINESS_RULES.md § "Fiscal year start") without discussing. Every prior report assumed July.` (this line also gets the §1 → section-title fix from Group A, applied at the same time).
    - Line 27: `"Retired the 'Tuesday upload bump' hypothesis — held at low confidence for 8 weeks, didn't strengthen."` → replace ` — ` with `; ` (this is a quoted example; keeping the structure but using semicolon preserves the example's shape).
    - Line 49: `Don't delete an old report from \`reports/\` to "clean up." The archive is more useful with every weak-looking week left in it — that's how you see the channel's real volatility.` → replace ` — ` with `; ` or split into two sentences. Recommended: `... with every weak-looking week left in it. That is how you see the channel's real volatility.`

    Per `CLAUDE.md` § "Voice": the contraction "don't" is fine in operator-facing prose, but if you split a sentence at an em dash, the new sentence may read better with "Do not" depending on rhythm. Use judgment; the rule that matters is zero em dashes after editing.

    Final pass: `grep -nF '—' docs/maintenance.md` returns ZERO matches. `grep -nE 'BUSINESS_RULES\.md §[0-9]+' docs/maintenance.md` returns ZERO matches.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; ! grep -nE 'BUSINESS_RULES\.md §[0-9]+' docs/maintenance.md; grep -cE 'BUSINESS_RULES\.md § "' docs/maintenance.md; ! grep -nF '—' docs/maintenance.md; ! grep -nF '–' docs/maintenance.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE 'BUSINESS_RULES\.md §[0-9]+' docs/maintenance.md` returns ZERO matches (no more §N references to BUSINESS_RULES.md).
    - `grep -cE 'BUSINESS_RULES\.md § "' docs/maintenance.md` returns at least 2 (the two §6 occurrences in line 9 both get the section-title form).
    - `grep -nF '—' docs/maintenance.md` returns ZERO matches (all five em dashes stripped per W-1).
    - `grep -nF '–' docs/maintenance.md` returns ZERO matches (no en dashes as punctuation either).
    - No banned vocabulary introduced (`grep -niE 'leverage|robust|delve|seamless|tapestry|multifaceted' docs/maintenance.md` returns 0).
    - Diff is scoped to cross-ref fixes and em-dash replacements; no other content changes (`git diff --stat docs/maintenance.md` shows a small change confined to the affected lines).
  </acceptance_criteria>

  <done>
    `docs/maintenance.md` references `BUSINESS_RULES.md` by section title, not section number. All five em dashes (lines 8, 10, 21, 27, 49 of the pre-edit file) are replaced with appropriate punctuation per CLAUDE.md § "Voice". The cross-reference rot tracked in CONCERNS.md is closed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add ERR-03 closing instruction to the recipe's failure-handling step</name>
  <files>.claude/commands/run-analyzer.md</files>

  <read_first>
    - `.claude/commands/run-analyzer.md` Step 11 (Operator message) where the three operator-message patterns (SUCCESS / NOTION-FAIL / BQ-FAIL) live. **Navigate by string anchor, not line number** — Plan 03-03 (Wave 2) will have shifted recipe line numbers, so locate Step 11 by searching for the literal heading `## Step 11: Operator message`. ERR-03's anchoring sentence rides at the end of this step because NOTION-FAIL and BQ-FAIL each name a runbook section the operator is about to read.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-CONTEXT.md` § "Claude's Discretion" → "ERR-03 mechanism" for the rule: "The slash command's failure-handling step should also include a closing instruction: 'If this failure mode is not in docs/runbook.md, add it as part of the fix.'"
    - `docs/maintenance.md` (post-Task-2 state) — verify whether a section literally titled "Adding a new runbook section" exists. If not (Task 2 didn't add one and the pre-edit file didn't have one), the closing sentence below uses the generic `(per docs/maintenance.md; ERR-03)` form.
    - `CLAUDE.md` § "Voice" for prose constraints.
  </read_first>

  <action>
    Locate Step 11 by string-search (`grep -n '^## Step 11:' .claude/commands/run-analyzer.md`). Add ONE sentence to the end of Step 11, immediately after the existing prose that follows the three operator-message bullet patterns and the closing sentence about exhaustive triple coverage. The sentence:

    > For both NOTION-FAIL and BQ-FAIL: if this failure mode is not in `docs/runbook.md`, add it as part of the fix (per `docs/maintenance.md`; ERR-03). The runbook only helps if it stays current.

    If `docs/maintenance.md` (post-Task-2) does contain a section literally titled "Adding a new runbook section", change the parenthetical to `(per docs/maintenance.md § "Adding a new runbook section"; ERR-03)`. Otherwise keep the generic form above. Executor verifies by reading the current state of maintenance.md before writing the sentence.

    Per `CLAUDE.md` § "Voice": no em dashes, no banned vocab.

    **Line-number robustness note:** every prior Plan 03-03 edit to the recipe (CSV branch, dispatch-call-site additions, dereferencing six `.planning/` refs) shifts line numbers. Do not rely on any specific line number; locate the insertion point by searching for the unique string `## Step 11: Operator message` and append the sentence after the existing closing prose for that step.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; grep -F 'If this failure mode is not in' .claude/commands/run-analyzer.md; grep -F 'ERR-03' .claude/commands/run-analyzer.md; awk '/^## Step 11:/{flag=1} /^## /&&!/^## Step 11:/{if(flag&&already_started){flag=0}; already_started=1} flag&&/If this failure mode is not in/{found=1} END{exit !found}' .claude/commands/run-analyzer.md &amp;&amp; echo "anchor in Step 11 OK"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -F 'If this failure mode is not in' .claude/commands/run-analyzer.md` returns 1 match (the closing instruction is present in Step 11).
    - `grep -F 'ERR-03' .claude/commands/run-analyzer.md` returns at least 1 match (the requirement ID is cited in the parenthetical for traceability).
    - The new sentence lands within Step 11 (the awk-based gate above confirms; alternatively, `grep -n 'Step 11\|If this failure mode is not in' .claude/commands/run-analyzer.md` shows the new line AFTER the "Step 11" heading line and BEFORE the next `## ` heading or EOF).
    - No em dashes added to the recipe.
    - The three SUCCESS / NOTION-FAIL / BQ-FAIL bullets are unchanged.
  </acceptance_criteria>

  <done>
    Recipe Step 11 closes with the ERR-03 anchoring sentence; future failure modes hit by the operator are explicitly required to land in the runbook as part of the fix. Located by string anchor, not line number, so Plan 03-03's recipe edits don't break this task.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add CHANGELOG entry covering Plan 03-04's edits</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - `CHANGELOG.md` (first 100 lines) to see Plans 03-01/02/03 entries and confirm the existing H2-date-then-bullets pattern.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-PATTERNS.md` § "CHANGELOG discipline" for the entry conventions.
    - `CLAUDE.md` § "Voice" for prose constraints.
  </read_first>

  <action>
    Append the Plan 03-04 entries to `CHANGELOG.md`. Add bullets under today's H2-date section (or create one if not already present from prior Wave 3 work).

    Five bullets, one per file changed (runbook + maintenance + recipe + schedule + smoke-test artifact):

    - `docs/runbook.md` (Plan 03-04): expanded from 6 named sections to 13+ named sections, covering every error category the recipe emits (env_missing, env_invalid, bq_auth, missing_table, empty_result, no_bigquery_transport, report_dict_invalid, skill_unavailable, transport_error, parent_not_found, permission_denied) and every cloud-specific failure mode (BigQuery MCP connector not authorized, Notion connector not authorized, routine env var missing in cloud config, routine timed out or hung, Anthropic UI shows error before recipe runs). The `env_invalid` category folds into the existing "Required environment variable is missing" section as a labeled sub-bullet (identical recovery path; separate section would split one fix into two). Each new section follows the Symptom / Fix / Recording template. Also fixed the portability bug at the old line 23 (the "service account key" Fix paragraph conflicted with D-01; replaced with a pointer to the new "BigQuery MCP connector not authorized" section). Before: 6 sections, conflicting cloud-auth guidance. After: 13+ sections, every recipe error category has a recovery path. Impact on recent reports: zero. Forward-looking: cloud routine smoke test (D-03, SCHED-02) and any future failure mode now have named documentation targets.
    - `docs/maintenance.md` (Plan 03-04): replaced `BUSINESS_RULES.md §N` numeric references with section-title references (`§ "<title>"`); stripped five em dashes at the pre-edit lines 8/10/21/27/49 per CLAUDE.md § "Voice" (W-1 fold-in from plan-checker feedback). Before: drift between maintenance.md's `§6` and BUSINESS_RULES.md's current section numbering, plus five em-dash voice violations. After: cross-reference rot closed, voice rules satisfied; future BUSINESS_RULES.md renumbering won't silently break maintenance.md. Impact on recent reports: zero.
    - `.claude/commands/run-analyzer.md` (Plan 03-04): added a single closing sentence to Step 11 (Operator message) anchoring ERR-03: "If this failure mode is not in `docs/runbook.md`, add it as part of the fix." Before: ERR-03 lived only in `docs/maintenance.md` as a general principle. After: the recipe itself reminds the operator to update the runbook on every failure. Impact on recent reports: zero.
    - `docs/schedule.md` (Plan 03-04, W-3 fold-in): Run-now checklist items (b), (c), (d) updated to cite the exact runbook section anchors Task 1 created (e.g., item (c) now links to `docs/runbook.md § "Routine run timed out or hung"` and `§ "Anthropic UI shows error before recipe runs"`). Before: checklist links pointed at sections that did not exist yet (forward references to Plan 03-04 work). After: the cross-doc link loop is closed; an operator following the Run-now checklist after a failure can navigate to a real runbook section. Impact on recent reports: zero.
    - `.planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` (Plan 03-04, NEW): captures the operator-executed SCHED-02 verification. Records pass/fail for each of the four Run-now checklist items (a/b/c/d), date of the test, and any notes. The phase verifier reads this file as the executable evidence that SCHED-02 holds; without it the requirement has no owner. Impact on recent reports: zero.

    Per `CLAUDE.md` § "Voice": no em dashes, no banned vocab.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; head -120 CHANGELOG.md | grep -c 'Plan 03-04'; head -120 CHANGELOG.md | grep -c 'docs/runbook.md'; head -120 CHANGELOG.md | grep -c 'docs/maintenance.md'; head -120 CHANGELOG.md | grep -c 'docs/schedule.md'; head -120 CHANGELOG.md | grep -c '03-CLOUD-SMOKE-TEST.md'; head -120 CHANGELOG.md | grep -c 'ERR-03'; head -120 CHANGELOG.md | grep -c 'SCHED-02'</automated>
  </verify>

  <acceptance_criteria>
    - `head -120 CHANGELOG.md` contains the phrase "Plan 03-04" at least once.
    - `head -120 CHANGELOG.md` contains the file paths `docs/runbook.md`, `docs/maintenance.md`, `.claude/commands/run-analyzer.md`, `docs/schedule.md`, and `03-CLOUD-SMOKE-TEST.md` each at least once in the new section.
    - `head -120 CHANGELOG.md` contains `ERR-03` at least once (citing the requirement ID).
    - `head -120 CHANGELOG.md` contains `SCHED-02` at least once (citing the new requirement this plan owns).
    - Each new bullet ends with an `Impact on recent reports:` clause.
    - No em dashes in new content.
    - No banned vocabulary in new content.
    - No duplicate H2 date heading if Plan 03-03's date matches today's date.
  </acceptance_criteria>

  <done>
    `CHANGELOG.md` documents Plan 03-04's five edits under today's date with the standard bullet shape, including the SCHED-02 cloud smoke-test artifact and the W-1/W-3 fold-ins.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update docs/schedule.md Run-now checklist anchors to match runbook sections created in Task 1 (W-3 fold-in)</name>
  <files>docs/schedule.md</files>

  <read_first>
    - `docs/schedule.md` (post-Plan-03-03 state, Run-now checklist section) for the current four checklist items (a)/(b)/(c)/(d). Plan 03-03 wrote item (a) linked to "Notion write failed" and items (b)/(c)/(d) without specific runbook anchors. This task wires items (b)/(c)/(d) to the new sections Task 1 created.
    - `docs/runbook.md` (post-Task-1 state of THIS plan) for the exact heading text Task 1 created. Use these heading strings verbatim.
    - `CLAUDE.md` § "Voice" for prose constraints (the checklist is operator-facing).
  </read_first>

  <action>
    Locate the Run-now checklist section in `docs/schedule.md` by string-search (`grep -n '## Run-now checklist' docs/schedule.md`). Modify the four checkbox items so each one cites the runbook anchor an operator would navigate to on failure. Replacement bodies:

    - **(a)** Leave the existing anchor (`docs/runbook.md § "Notion write failed"`) as-is — Plan 03-03 wired it correctly. Optionally augment to also reference the new `§ "Notion connector not authorized"` anchor (cloud-specific case): "If not, see `docs/runbook.md` § 'Notion write failed' (local) or § 'Notion connector not authorized' (cloud)."
    - **(b)** Keep the existing spot-check guidance about Notion rendering. Append: "If sections render with stripped formatting or broken parentheticals, see `docs/runbook.md` § 'Report says something the operator believes is wrong' for the audit-trail-first recovery path."
    - **(c)** Replace the existing closing guidance with: "If the routine run fails to complete OR completes with `category: ...` error lines in the transcript, see `docs/runbook.md` § 'Routine run timed out or hung' (for unfinished runs) or § 'Anthropic UI shows error before recipe runs' (for infrastructure errors before Step 1)."
    - **(d)** Append: "If `summary.json.notion_write_ok` is `false`, the value in `errors[].category` names the recovery section: `env_missing` → § 'Routine environment variable missing in cloud config'; `transport_error` / `parent_not_found` / `permission_denied` → § 'Notion connector not authorized'; `no_bigquery_transport` → § 'No BigQuery transport available'; `skill_unavailable` → § 'write-notion-report Skill not loaded in the session'; `report_dict_invalid` → § 'Skill input dict missing a required key'. All sections live in `docs/runbook.md`."

    Also update the closing sentence of the Run-now checklist section (currently mentions "New failure modes ... get their own runbook sections in Plan 03-04"). Replace with: "If any check fails, the linked runbook section names the recovery. New failure modes get added to `docs/runbook.md` and `CHANGELOG.md` as part of the fix (ERR-03)."

    Per `CLAUDE.md` § "Voice": no em dashes, no banned vocab. The arrow character `→` is acceptable in operator-facing checklists (it is not an em dash and does not trigger the voice rule); if your reviewer prefers ASCII, substitute `->`.
  </action>

  <verify>
    <automated>cd /Users/kylechalmers/Development/channel-patterns-analyzer; grep -c 'Routine run timed out or hung' docs/schedule.md; grep -c 'Anthropic UI shows error before recipe runs' docs/schedule.md; grep -c 'No BigQuery transport available' docs/schedule.md; grep -c 'write-notion-report Skill not loaded' docs/schedule.md; grep -c 'Skill input dict missing a required key' docs/schedule.md; grep -c 'Notion connector not authorized' docs/schedule.md; grep -c 'Routine environment variable missing in cloud config' docs/schedule.md; grep -c 'ERR-03' docs/schedule.md; ! grep -nF '—' docs/schedule.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -F 'Routine run timed out or hung' docs/schedule.md` returns at least 1 (item (c) wired).
    - `grep -F 'Anthropic UI shows error before recipe runs' docs/schedule.md` returns at least 1 (item (c) wired).
    - `grep -F 'No BigQuery transport available' docs/schedule.md` returns at least 1 (item (d) wired).
    - `grep -F 'write-notion-report Skill not loaded' docs/schedule.md` returns at least 1 (item (d) wired).
    - `grep -F 'Skill input dict missing a required key' docs/schedule.md` returns at least 1 (item (d) wired).
    - `grep -F 'Notion connector not authorized' docs/schedule.md` returns at least 1 (item (a) and/or (d) wired).
    - `grep -F 'Routine environment variable missing in cloud config' docs/schedule.md` returns at least 1 (item (d) wired).
    - `grep -F 'ERR-03' docs/schedule.md` returns at least 1 (the closing sentence cites the requirement ID).
    - `grep -nF '—' docs/schedule.md` returns 0 (no em dashes added; the file was clean pre-edit).
    - The four checkbox items still exist (`grep -cE '^- \[ \] \([a-d]\)' docs/schedule.md` returns exactly 4).
  </acceptance_criteria>

  <done>
    Run-now checklist items (a)/(b)/(c)/(d) in `docs/schedule.md` cite the exact runbook section anchors Task 1 created. The cross-doc link loop is closed; an operator hitting a failure during the Run-now smoke test navigates to a real recovery page, not a forward reference.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: SCHED-02 cloud smoke test (manual-verify, owns SCHED-02 verification)</name>

  <what-built>
    Tasks 1-5 of this plan AND all prior Wave 1-2 work (Plans 03-01 / 03-02 / 03-03) are complete. The cloud routine `channel-patterns-analyzer-weekly` exists in claude.com (per Plan 03-03 Task 2's setup walkthrough). All four Run-now checklist items in `docs/schedule.md` have runbook anchors that resolve to real sections (per Task 5 above). The `write-notion-report` Skill exists in `.claude/skills/write-notion-report/`. The operator has not yet run the cloud routine end-to-end against live BigQuery + Notion.

    The SCHED-02 requirement ("the Notion writer Skill works identically in both contexts (local MCP, cloud connector) without code changes") cannot be verified by any executable test the executor can run unattended. It requires the operator to click "Run now" in the live claude.com UI, wait ~3 minutes, and verify four pass/fail items.

    This task is the SCHED-02 verification owner. Without a recorded result, the phase verifier has no executable evidence that the cloud path works, and the phase cannot close.
  </what-built>

  <how-to-verify>
    Step 1: Create the smoke-test record file. Before clicking Run now, create `.planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` with this template:

    ```
    # Phase 3 Cloud Smoke Test (SCHED-02)

    **Routine:** channel-patterns-analyzer-weekly
    **Test date:** {YYYY-MM-DD when Run now is clicked}
    **Operator:** Kyle Chalmers
    **Run trigger:** Manual "Run now" from claude.com routine detail page

    ## Run-now checklist results (per docs/schedule.md § "Run-now checklist")

    ### Item (a): Notion child page appears within 60 seconds
    - Status: [ pass | fail ]
    - Evidence: {link to Notion child page URL, OR error description}
    - Notes:

    ### Item (b): Notion page renders all six sections with parentheticals intact
    - Status: [ pass | fail ]
    - Sections present: [ Data Health | Headline | What is working | What is not working | Patterns worth watching | Open questions ] (tick each)
    - Parenthetical spot-check: {one example finding with (label, n=N) rendered as plain text}
    - Notes:

    ### Item (c): Anthropic UI shows the routine run as completed (green)
    - Status: [ pass | fail ]
    - Run transcript URL:
    - Any `category: ...` error lines: { none | list them }
    - Notes:

    ### Item (d): summary.json exists with notion_write_ok=true and empty errors
    - Status: [ pass | fail ]
    - Path: runs/{YYYY-MM-DD}/summary.json
    - notion_write_ok: { true | false }
    - errors[]: { [] | list error categories }
    - notion_url: {URL or null}
    - Notes:

    ## Overall SCHED-02 verdict
    - [ ] All four items PASS — SCHED-02 verified, phase can close
    - [ ] One or more items FAIL — see notes; phase blocked until resolved

    ## Failure recovery (if any item failed)
    - Runbook section navigated to:
    - Recovery taken:
    - Re-test required: [ yes | no ]
    ```

    Step 2: Open claude.com, navigate to Settings → Routines → channel-patterns-analyzer-weekly, and click "Run now". Wait up to 3 minutes.

    Step 3: Walk the four checklist items per `docs/schedule.md § "Run-now checklist"` exactly as written. For each item, record pass/fail and evidence in `03-CLOUD-SMOKE-TEST.md`.

    Step 4: If any item failed, navigate to the linked runbook section, attempt the recovery, and either (a) re-run and re-test, recording in the same file, OR (b) record the failure as a phase-blocker if recovery is non-trivial.

    Step 5: If all four pass, fill in "Overall SCHED-02 verdict" with the all-pass checkbox ticked. Commit `03-CLOUD-SMOKE-TEST.md` to the repo (it lives in `.planning/` which is committed).

    Step 6: Report back with one of:
    - `approved` — all four items pass, SCHED-02 verified, smoke-test file committed
    - `blocked: <reason>` — describe what failed and what the runbook recovery looked like
    - `deferred: <reason>` — operator unavailable to run live; record why and when re-test will happen (phase verifier will reject this unless documented)
  </how-to-verify>

  <acceptance_criteria>
    - `.planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` exists with the template above filled in for all four items.
    - The "Overall SCHED-02 verdict" section has exactly one checkbox ticked.
    - If verdict is "all four PASS", `git log --oneline -1 -- .planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` shows a commit.
    - If verdict is "FAIL", the failure-recovery section names the runbook anchor used and the next action.
    - `grep -F 'SCHED-02' .planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` returns at least 1 (the requirement ID is in the file header).
  </acceptance_criteria>

  <resume-signal>Type `approved` if all four items passed and SCHED-02 is verified. Type `blocked: <reason>` if any item failed. Type `deferred: <reason>` only if you cannot run the live smoke test today (this defers phase close).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation prose → operator reading on a Monday morning | Information-disclosure boundary; secrets in committed docs would leak; internal URLs in "Recording" lines could leak org structure |
| Live cloud routine → real Notion workspace (Task 6) | Side-effect boundary; the smoke test writes a real Notion child page to Kyle's channel-patterns parent. Acceptable because the page is part of the audit trail; if the test fires twice, two child pages appear, which is intended (each Run now is a distinct run record) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information disclosure | PII / customer data in runbook examples | mitigate | All new runbook examples use synthetic identifiers or placeholder forms (`<your GCP project id>`, `BQ_PROJECT`, `NOTION_REPORT_PAGE_ID`). No real channel IDs, no real video titles in example error messages. Task 1 acceptance criteria do not include a real-data check explicitly, but reviewer verifies by reading the new sections for any concrete real-world value before merging |
| T-03-04-02 | Information disclosure | Internal URLs in "Recording" lines | mitigate | "Recording" lines reference `CHANGELOG.md` (public, in-repo) and generic counters ("next expiry"); no org-specific URLs added. The one external URL added is `https://claude.com` (public service) |
| T-03-04-03 | Repudiation | Operator skipping the ERR-03 closing instruction in Step 11 | accept | The instruction is markdown discipline, not automated enforcement. Per CONTEXT.md D-04's accepted risk for D-04 ("manual discipline, no automation, no slash-command-driven runbook prompt"), the strongest enforcement available is the recipe sentence itself plus the existing `docs/maintenance.md` rule |
| T-03-04-04 | Information disclosure | SCHED-02 smoke-test artifact (`03-CLOUD-SMOKE-TEST.md`) | mitigate | The template uses placeholder URLs (operator fills in real ones). The committed file may contain a real Notion URL for the smoke-test child page; this is acceptable because Notion URLs are unguessable UUIDs and the channel-patterns parent is private to Kyle's workspace. Do NOT include any `BQ_PROJECT` value or routine env-var values in the file |
| T-03-04-SC | Tampering | new package installs (npm/pip/cargo) | N/A | No new dependencies; docs-only plan plus a single recipe sentence plus an operator-filled markdown artifact. The supply-chain threat does not apply |

**Threats explicitly NOT applicable to this plan:**
- **SQL injection**: N/A. No SQL added.
- **XSS**: N/A. No web UI.
- **Authn/authz**: N/A. No auth surface added; the new sections DOCUMENT existing auth surfaces (BigQuery connector, Notion connector) without creating new ones.
- **CSRF**: N/A. No state-changing endpoint.
- **Path traversal**: N/A. No file paths added that take operator input.

</threat_model>

<verification>

- `grep -nF 'service account key' docs/runbook.md` returns zero matches (portability bug fixed).
- `grep -nE 'BUSINESS_RULES\.md §[0-9]+' docs/maintenance.md` returns zero matches (cross-ref rot fixed).
- `grep -nF '—' docs/maintenance.md` returns zero matches (em dashes stripped per W-1).
- Every recipe error category appears as the suffix of a runbook H2 heading or as a clearly-labeled recovery target within an existing section (the Task 1 category-coverage loop is the executable check).
- Every Run-now checklist link in `docs/schedule.md` (post-Task-5) resolves to a real section in the updated `docs/runbook.md` (cross-doc link integrity).
- `CHANGELOG.md` has a new section under today's date documenting Plan 03-04 with five file-level bullets including the smoke-test artifact and SCHED-02 citation.
- Recipe Step 11 carries the ERR-03 anchoring sentence (`grep -F 'If this failure mode is not in' .claude/commands/run-analyzer.md` returns 1).
- `.planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` exists with all four Run-now checklist items recorded as pass/fail with evidence (Task 6 owns SCHED-02).
- The analyzer remains end-to-end runnable in CSV mode and BigQuery mode after this plan ships (smoke-test by running once in each mode; no code paths changed apart from one sentence in Step 11).

</verification>

<success_criteria>

1. `docs/runbook.md` has at least 12 named H2 sections (6 pre-existing + at least 6 new for cloud + operator-note + error-category coverage).
2. Every recipe error category (env_missing, env_invalid, bq_auth, missing_table, empty_result, no_bigquery_transport, report_dict_invalid, skill_unavailable, transport_error, parent_not_found, permission_denied) is grep-findable in `docs/runbook.md` and maps to a named section (`env_invalid` folds into the existing "Required environment variable is missing" section as a sub-bullet).
3. The five cloud-specific failure modes RESEARCH.md predicted have dedicated runbook sections following Symptom / Fix / Recording.
4. The "service account key" portability bug in `docs/runbook.md` is fixed and replaced with a pointer to the new "BigQuery MCP connector not authorized" section.
5. `BUSINESS_RULES.md §N` numeric references in `docs/maintenance.md` are replaced with section-title references; five em dashes stripped per CLAUDE.md voice rules.
6. The recipe's Step 11 closing instruction anchors ERR-03 ("If this failure mode is not in `docs/runbook.md`, add it as part of the fix").
7. `docs/schedule.md` Run-now checklist items (a)/(b)/(c)/(d) cite the exact runbook section anchors created by Task 1 (W-3 cross-doc link loop closed).
8. `CHANGELOG.md` documents the five edits under today's date.
9. `.planning/phases/03-csv-parity-and-operational-polish/03-CLOUD-SMOKE-TEST.md` exists with the four Run-now checklist items recorded as pass/fail (SCHED-02 verified).
10. No em dashes, no banned vocabulary, no formulaic openers in any new content.
11. Analyzer remains end-to-end runnable in CSV-mode and BigQuery-mode (no code paths changed; sanity-check by reading the recipe's Step 1 through 11 unchanged from Plan 03-03's state apart from the single Step 11 sentence).

</success_criteria>

<output>
Create `.planning/phases/03-csv-parity-and-operational-polish/03-04-SUMMARY.md` when done, documenting: total H2 section count in runbook.md before/after, the list of new section headings added, the category-coverage loop result (all 11 categories grep-findable), which `BUSINESS_RULES.md §N` reference(s) were fixed in maintenance.md and what they were replaced with, the five em dashes stripped in maintenance.md (line numbers in pre-edit file), the exact ERR-03 sentence added to Step 11 of the recipe, the Run-now checklist anchor updates in schedule.md, the SCHED-02 cloud smoke test result (or status: ran/deferred), and any failure modes the executor decided to defer (with rationale).
</output>
