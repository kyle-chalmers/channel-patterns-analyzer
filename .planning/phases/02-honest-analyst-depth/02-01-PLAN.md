---
phase: 02-honest-analyst-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/02_top_full_length_videos.sql
  - sql/03_age_controlled_performance.sql
  - sql/04_data_health_check.sql
  - CHANGELOG.md
  # Planning artifact (gitignored under .planning/); produced by Task 1 so Plans 02 and 03 can read it.
  - .planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md
autonomous: true
requirements:
  - ANALYSIS-01
  - ANALYSIS-02
  - ANALYSIS-04
user_setup: []
tags:
  - bigquery
  - sql
  - analyzer

must_haves:
  truths:
    - "sql/02, sql/03, sql/04 use CURRENT_DATE('America/Phoenix') everywhere they previously used bare CURRENT_DATE()"
    - "sql/02 and sql/03 filter rows via a latest_common CTE that takes LEAST(MAX(snapshot_date) of video_metadata, MAX(snapshot_date) of daily_video_stats)"
    - "sql/02 and sql/03 no longer carry LIMIT 20"
    - "Each modified SQL file passes bq dry-run validation"
    - "CHANGELOG.md has a single dated H2 entry for today with one bullet per SQL file changed, naming the before/after behavior"
    - "Phase 1 dependency assumptions (A1 recipe seam, A2 Skill input contract, A3 summary.json additive-friendly) are verified against Phase 1's actual outputs and the verification recorded for Plans 02 and 03"
  artifacts:
    - path: sql/02_top_full_length_videos.sql
      provides: "Top full-length videos by views, correctly joined and Phoenix-time aware"
      contains: "CURRENT_DATE(\"America/Phoenix\")"
    - path: sql/03_age_controlled_performance.sql
      provides: "Age-controlled per-video view rate, correctly joined and Phoenix-time aware"
      contains: "CURRENT_DATE(\"America/Phoenix\")"
    - path: sql/04_data_health_check.sql
      provides: "Per-table staleness in Phoenix time"
      contains: "CURRENT_DATE(\"America/Phoenix\")"
    - path: CHANGELOG.md
      provides: "Dated audit entry for the three SQL fixes"
      contains: "## 2026-05-25"
  key_links:
    - from: "sql/02 latest_common CTE"
      to: "sql/01 latest_common CTE"
      via: "Borrowed LEAST(subquery, subquery) pattern; same two-table scope"
      pattern: "WITH latest_common AS"
    - from: "sql/03 latest_common CTE"
      to: "sql/01 latest_common CTE"
      via: "Same pattern as sql/02"
      pattern: "WITH latest_common AS"
---

<objective>
Fix the three SQL bugs flagged in CONCERNS.md and locked under CONTEXT.md D-05 so Phase 2's "honest analyst" report draws from numerically correct queries. Without this plan, Phase 2 would ship a report whose age math drifts by up to 7 hours at day boundaries and whose top-performer list silently drops rows when video_metadata and daily_video_stats land out of sync.

Purpose: Phase 2 wires CLAUDE.md's analytical contract into the recipe; the contract is only worth wiring if the underlying numbers are correct. Defer-and-Phase-3 was rejected in D-05 specifically because the wrong numbers would land in published reports.

Output:
- Three edited SQL files (Phoenix timezone, latest-common-snapshot CTE in 02 and 03, no LIMIT in 02 and 03)
- One dated CHANGELOG.md H2 entry with three bullets
- A Phase 1 dependency-assumption verification note saved to `.planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md` so Plans 02 and 03 know which Phase 1 surfaces they can rely on
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-honest-analyst-depth/02-CONTEXT.md
@.planning/phases/02-honest-analyst-depth/02-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md
@BUSINESS_RULES.md
@docs/maintenance.md
@sql/01_latest_snapshot_overview.sql
@sql/02_top_full_length_videos.sql
@sql/03_age_controlled_performance.sql
@sql/04_data_health_check.sql
@CHANGELOG.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify Phase 1 outputs and record dependency-assumption status</name>
  <read_first>
    - .planning/phases/02-honest-analyst-depth/02-RESEARCH.md (sections "Assumptions Log" — specifically A1, A2, A3, A5; "Architecture Patterns" diagram showing where Phase 2 inserts)
    - .planning/phases/01-first-notion-report-end-to-end/01-CONTEXT.md (D-02 recipe shape; Claude's Discretion on Skill input contract and report depth)
    - .planning/phases/01-first-notion-report-end-to-end/ (list contents; if `01-NN-PLAN.md` or `01-NN-SUMMARY.md` files exist, read them; if `.claude/commands/run-analyzer.md` exists, read it; if `.claude/skills/write-notion-report/SKILL.md` exists, read it)
    - runs/README.md (current summary.json schema — Phase 2 extends it under D-10 in Plan 02 and under Claude's Discretion in Plan 03)
  </read_first>
  <action>
    Verify the three Phase 1 dependency assumptions called out in 02-RESEARCH.md before any code touches the repo, and record the verification so Plans 02 and 03 can rely on it.

    1. Create `.planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md` with one section per assumption (A1, A2, A3, A5) using this exact structure for each:
       - `## Assumption A{N}: {one-line restatement}`
       - `**Status:** verified | partially-verified | not-yet-shipped | broken`
       - `**Evidence:** {what was read; file paths and line ranges OR "Phase 1 not yet shipped — assumption defaults to RESEARCH.md's stated default"}`
       - `**Impact on Plan 02/03:** {one to three lines naming the concrete consequence}`

       Cover exactly these assumptions:
       - **A1** (Plan dependency on D-04): "Phase 1 ships /run-analyzer as a linear recipe with a clean draft → publish seam." Check whether `.claude/commands/run-analyzer.md` exists. If yes, locate the boundary between the draft step and the `write-notion-report` invocation and quote the exact heading or line range. If no, note that Plan 02 will create the seam as part of inserting new steps and that the assumption is deferred until Phase 1 lands.
       - **A2** (Skill input contract uses structured per-finding records): Check whether `.claude/skills/write-notion-report/SKILL.md` exists. If yes, locate the input-contract section and record whether per-finding records are a structured dict or a single `markdown_body` string. If no, the default from RESEARCH.md (structured per-finding records with `{label, n}` confidence) holds. State the consequence: if Phase 1 ends up shipping a markdown-only contract, Plan 02's confidence labels are prose-only and Plan 03's `voice_audit` still works.
       - **A3** (`summary.json` writer is additive-friendly): Read runs/README.md as the contract. State whether the schema is documentation (additive-friendly) or enforced (closed). Note that Phase 2's additions in Plans 02 and 03 (`prior_reports_consulted`, `voice_audit`) will be additive.
       - **A5** (Notion `markdown` body param preserves `(label, n=N)` parentheticals): No way to verify until the first real Plan 03 run. Record as `not-yet-shipped`; state the fallback (Plan 03 surfaces the issue; Phase 1's Skill would switch to `children` blocks; out of Phase 2's scope to pre-mitigate).

    2. Do not block this task on Phase 1 absence. If `.claude/commands/run-analyzer.md` and the Skill file do not exist yet, that is an expected state (Phase 1 is in parallel planning per CONTEXT.md). Record `not-yet-shipped` and proceed.

    3. This file is a planning artifact under `.planning/` and is therefore gitignored. It exists for Plans 02 and 03 to read; no commit needed for it.

    This task implements the Phase 1 dependency caveat called out in the planning_context (RESEARCH.md A1/A2/A3/A5) so subsequent tasks don't repeat the verification work.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md &amp;&amp; grep -c '^## Assumption A' .planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md` exists.
    - File contains exactly four `## Assumption A` H2 headings (one each for A1, A2, A3, A5).
    - Each section contains a `**Status:**` line whose value is one of `verified`, `partially-verified`, `not-yet-shipped`, `broken`.
    - Each section contains `**Evidence:**` and `**Impact on Plan 02/03:**` lines.
    - If `.claude/commands/run-analyzer.md` is absent, A1's status is `not-yet-shipped` with evidence stating the absence; if present, A1's status reflects the actual seam shape.
  </acceptance_criteria>
  <done>
    Plans 02 and 03 can read PHASE1-ASSUMPTIONS-VERIFIED.md to know which Phase 1 surfaces they can rely on, without redoing the verification work.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix sql/02, sql/03, sql/04 per D-05 (Phoenix tz + latest-common-snapshot CTE + no LIMIT)</name>
  <read_first>
    - sql/01_latest_snapshot_overview.sql (the source pattern for `WITH latest_common AS (SELECT LEAST(...))` that Plan 01 borrows; copy the exact two-subquery shape — do not extend to four tables, per D-06)
    - sql/02_top_full_length_videos.sql (current state; preserve column list and ORDER BY; only changes are header comment, latest_common CTE, Phoenix tz in DATE_DIFF, and removal of LIMIT)
    - sql/03_age_controlled_performance.sql (current state; same three Phoenix-tz replacements — line 24 and line 35 in the current file — plus the latest_common CTE replacing the inline subquery on lines 31-33; preserve the `views_per_day_since_publish_proxy` column and its SAFE_DIVIDE math)
    - sql/04_data_health_check.sql (current state; four `CURRENT_DATE()` calls to replace with `CURRENT_DATE("America/Phoenix")`; header comment also needs the Phoenix-as-canonical update and the wrong `BUSINESS_RULES.md §5` reference replaced with the section title `BUSINESS_RULES.md § "Data health expectations"`)
    - .planning/phases/02-honest-analyst-depth/02-RESEARCH.md (section "Code Examples" — exact target shape for sql/02; section "Architectural Responsibility Map" confirming SQL is the right tier for the age filter and the LEAST CTE)
    - CLAUDE.md (sections "Age control is non-negotiable" and "Tooling notes" — the rules these SQL fixes serve)
    - BUSINESS_RULES.md (sections "Data health expectations" and "Table grain and join keys" — the contract sql/02, sql/03, sql/04 must respect; this is where the section-title reference points)
  </read_first>
  <action>
    Apply the D-05 fixes to all three SQL files in one task because they share the same pattern and reviewer load is lower as a single diff.

    **sql/02_top_full_length_videos.sql** — apply these exact changes:
    1. Header comment: replace the `BUSINESS_RULES.md §3` reference on line 4 with `CLAUDE.md § "Age control is non-negotiable"` (the rule actually lives in CLAUDE.md per CONCERNS.md). Add a paragraph after the dataset-name paragraph explaining the Phoenix-tz choice (cite `BUSINESS_RULES.md § "Data health expectations"` by section title, never by §N). Add a paragraph explaining the new latest-common-snapshot logic and that it spans only the two joined tables per D-06.
    2. Replace the bare `SELECT ...` block (lines 15-32) with a CTE-prefixed version: add `WITH latest_common AS (SELECT LEAST((SELECT MAX(snapshot_date) FROM \`youtube_analytics.video_metadata\`), (SELECT MAX(snapshot_date) FROM \`youtube_analytics.daily_video_stats\`)) AS snapshot_date)` at the top, then the existing SELECT/FROM/JOIN body.
    3. In the `WHERE` clause, replace `(SELECT MAX(snapshot_date) FROM \`youtube_analytics.video_metadata\`)` with `(SELECT snapshot_date FROM latest_common)`.
    4. Replace `CURRENT_DATE()` in the DATE_DIFF on line 20 with `CURRENT_DATE("America/Phoenix")`.
    5. Remove the `LIMIT 20` on line 32. Keep the trailing semicolon.

    **sql/03_age_controlled_performance.sql** — apply the same pattern adapted to the existing CTE structure:
    1. Header comment: replace both `BUSINESS_RULES.md §3` references (lines 2 and 10) with `CLAUDE.md § "Age control is non-negotiable"`. Keep the IMPORTANT block about the proxy semantics — that prose is still correct.
    2. Wrap the existing `base` CTE: introduce `latest_common` as the first CTE (same two-subquery LEAST shape as sql/02), then have `base` reference `(SELECT snapshot_date FROM latest_common)` instead of `(SELECT MAX(snapshot_date) FROM \`youtube_analytics.video_metadata\`)` on lines 31-33.
    3. Replace `CURRENT_DATE()` in both DATE_DIFF calls (line 24 in the SELECT list and line 35 in the WHERE clause) with `CURRENT_DATE("America/Phoenix")`.
    4. Remove the `LIMIT 20` on line 47.

    **sql/04_data_health_check.sql** — Phoenix-tz only (no CTE work; this file does not join):
    1. Header comment: replace the broken `BUSINESS_RULES.md §5` reference on line 2 with `BUSINESS_RULES.md § "Data health expectations"` (per CONCERNS.md, §5 does not exist). Remove the "if your scheduling timezone differs" conditional language on lines 9-12 and replace with a statement that Phoenix is the canonical timezone per the section just cited.
    2. Replace all four `CURRENT_DATE()` calls (lines 17, 25, 33, 41) with `CURRENT_DATE("America/Phoenix")`. Each call sits inside a `DATE_DIFF(...)` second argument; the first argument is unchanged.
    3. Do not touch the UNION ALL structure, the table order, or the ORDER BY.

    Do NOT in this task: extend sql/01's LEAST to four tables (D-06 defers this); template `${BQ_DATASET}` (D-06 defers this to Phase 1's BQ-01); change column names; add or remove tables from the UNION in sql/04.

    These changes implement D-05 from CONTEXT.md and unblock ANALYSIS-01, ANALYSIS-02, and ANALYSIS-04 by ensuring the 14-day age filter in sql/03 fires off Phoenix-local dates and the join in sql/02 and sql/03 doesn't silently drop rows when daily_video_stats lands at a different snapshot than video_metadata.
  </action>
  <verify>
    <automated>[ $(grep -c 'CURRENT_DATE("America/Phoenix")' sql/02_top_full_length_videos.sql) -ge 1 ] &amp;&amp; [ $(grep -c 'CURRENT_DATE("America/Phoenix")' sql/03_age_controlled_performance.sql) -ge 2 ] &amp;&amp; [ $(grep -c 'CURRENT_DATE("America/Phoenix")' sql/04_data_health_check.sql) -eq 4 ] &amp;&amp; ! grep -nE '\bCURRENT_DATE\(\)' sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql sql/04_data_health_check.sql &amp;&amp; ! grep -nE '^\s*LIMIT\s+20' sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql &amp;&amp; grep -q 'WITH latest_common AS' sql/02_top_full_length_videos.sql &amp;&amp; grep -q 'WITH latest_common AS' sql/03_age_controlled_performance.sql &amp;&amp; ! grep -E 'BUSINESS_RULES\.md §[0-9]' sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql sql/04_data_health_check.sql</automated>
  </verify>
  <acceptance_criteria>
    - `sql/02_top_full_length_videos.sql` contains at least one `CURRENT_DATE("America/Phoenix")`, zero bare `CURRENT_DATE()` calls, no `LIMIT 20` clause, and one `WITH latest_common AS` CTE.
    - `sql/03_age_controlled_performance.sql` contains at least two `CURRENT_DATE("America/Phoenix")` calls (one in SELECT-list DATE_DIFF, one in WHERE-clause DATE_DIFF), zero bare `CURRENT_DATE()` calls, no `LIMIT 20` clause, and one `WITH latest_common AS` CTE that precedes the existing `base` CTE.
    - `sql/04_data_health_check.sql` contains exactly four `CURRENT_DATE("America/Phoenix")` calls and zero bare `CURRENT_DATE()` calls.
    - None of the three files contains a `BUSINESS_RULES.md §N` numeric-section reference; all references use section titles.
    - `bq query --use_legacy_sql=false --dry_run < sql/02_top_full_length_videos.sql` exits 0 (validates syntax against live BigQuery).
    - `bq query --use_legacy_sql=false --dry_run < sql/03_age_controlled_performance.sql` exits 0.
    - `bq query --use_legacy_sql=false --dry_run < sql/04_data_health_check.sql` exits 0.
    - The latest_common CTE in sql/02 and sql/03 references only `video_metadata` and `daily_video_stats` (the two joined tables); it does not reference `daily_video_analytics` or `daily_traffic_sources` (D-06 defers four-table extension).
  </acceptance_criteria>
  <done>
    The three canonical SQL files Phase 2 depends on produce correct numbers: Phoenix-time age math, latest-common-snapshot join filter (no silent row drops when tables land out of sync), and no LIMIT 20 ceiling. All three files pass `bq` dry-run validation. No `BUSINESS_RULES.md §N` numeric references remain in the modified files.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add CHANGELOG.md entry for the three SQL fixes per docs/maintenance.md</name>
  <read_first>
    - CHANGELOG.md (current state; copy the existing H2 date heading style and bullet formatting; new entry goes at the top of the file under the existing entries header)
    - docs/maintenance.md (section "Evolving a business rule" — required format: state the change in plain language, name before/after values for any threshold moved, say whether the change would have changed a recent report's numbers; section "Adding a new SQL query" — the convention also applies to edits)
    - .planning/codebase/STRUCTURE.md (section "Naming Conventions" — "CHANGELOG entries are dated H2 headings (`## 2026-05-25`) with bulleted changes underneath")
  </read_first>
  <action>
    Append a single dated H2 entry to CHANGELOG.md per the format documented in `docs/maintenance.md` § "Evolving a business rule" and `.planning/codebase/STRUCTURE.md` § "Naming Conventions". Use today's date (`2026-05-25`) as the H2 heading.

    Required structure:
    - `## 2026-05-25` heading (or whatever today's date resolves to in `America/Phoenix` at run time; use `date +%Y-%m-%d` if needed)
    - Three bullets, one per SQL file changed. Each bullet states the before behavior and the after behavior in one sentence so a future auditor reading the changelog can understand the report-number impact without diffing the SQL.

    Suggested bullet wording (adapt as needed; the wording is not the verified contract, the structure is):
    - `sql/02_top_full_length_videos.sql`: before — joined on \`video_metadata\`'s latest snapshot only, used UTC \`CURRENT_DATE()\` for age math, capped at \`LIMIT 20\`; after — joins on the LEAST common snapshot of \`video_metadata\` and \`daily_video_stats\`, uses \`CURRENT_DATE("America/Phoenix")\`, no row limit. Impact on recent reports: would have changed top-N membership on any day where the two source tables landed at different snapshots; no prior published reports exist yet, so no retroactive change.
    - `sql/03_age_controlled_performance.sql`: same three changes as sql/02 plus the 14-day age filter now fires off Phoenix-local dates (previously could mis-classify a video as `days_since_published = 14` versus 13 for up to seven hours per day around the UTC day boundary).
    - `sql/04_data_health_check.sql`: replaced four bare \`CURRENT_DATE()\` calls with \`CURRENT_DATE("America/Phoenix")\` so the `days_stale` math aligns with the upstream `youtube-bigquery-pipeline` Phoenix scheduler per `BUSINESS_RULES.md § "Data health expectations"`. Also fixed the broken `BUSINESS_RULES.md §5` reference in the header (the section title now lives in the file directly).

    Do not add bullets for the planned recipe edits in Plan 02 or the self-audit step in Plan 03 — those are separate behavior changes and each owns its own CHANGELOG entry.

    This entry implements the `docs/maintenance.md` § "Evolving a business rule" requirement that SQL-file edits which change report numbers are logged.
  </action>
  <verify>
    <automated>grep -q '^## 2026-05-25$' CHANGELOG.md &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -c '^-' | awk '$1&gt;=3{exit 0} {exit 1}' &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -qE 'sql/02' &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -qE 'sql/03' &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -qE 'sql/04'</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a `## 2026-05-25` H2 heading.
    - Under that heading and before the next `## ` heading, there are at least three bulleted lines (each starting with `-`).
    - The bullets named in those lines reference `sql/02`, `sql/03`, and `sql/04` by filename.
    - The new heading is the most recent date in the file (placed at the top of the entries section per existing convention).
    - No other H2 heading in CHANGELOG.md has been modified.
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a single new dated entry that names the three SQL files changed, the before/after behavior, and the report-number impact. Future auditors can read the entry without diffing the SQL to understand why numbers shifted between this and the prior report.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `bq query --use_legacy_sql=false --dry_run < sql/02_top_full_length_videos.sql` exits 0.
2. `bq query --use_legacy_sql=false --dry_run < sql/03_age_controlled_performance.sql` exits 0.
3. `bq query --use_legacy_sql=false --dry_run < sql/04_data_health_check.sql` exits 0.
4. `grep -nE 'CURRENT_DATE\(\)' sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql sql/04_data_health_check.sql` returns no matches (no bare CURRENT_DATE remains).
5. `grep -nE 'LIMIT 20' sql/02_top_full_length_videos.sql sql/03_age_controlled_performance.sql` returns no matches.
6. CHANGELOG.md has a single `## 2026-05-25` entry with three SQL-file bullets.
7. `.planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md` exists with four `## Assumption A` headings.

Optional smoke test (only if Phase 1's `/run-analyzer` recipe exists in `.claude/commands/run-analyzer.md`): invoke it end-to-end and verify the report's data-health staleness numbers match what `bq query < sql/04_data_health_check.sql` returns directly when run with the same Phoenix-local clock. This smoke test is the live integration check; do not run it if Phase 1 hasn't shipped (the recipe doesn't exist yet).
</verification>

<success_criteria>
- All three SQL files use `CURRENT_DATE("America/Phoenix")` exclusively.
- sql/02 and sql/03 use the latest_common CTE pattern borrowed from sql/01, scoped to the two joined tables.
- sql/02 and sql/03 have no `LIMIT 20` clause.
- All three files pass `bq` dry-run validation.
- CHANGELOG.md has a dated entry naming each file change with before/after behavior.
- Phase 1 dependency assumptions are recorded for Plans 02 and 03.
- This plan addresses requirements ANALYSIS-01 (the 14-day filter now fires on Phoenix-local dates), ANALYSIS-02 (sql/03's proxy now produces numerically correct views-per-day), and ANALYSIS-04 (trending claims gated by the same correctly-filtered ≥14 day age set).
</success_criteria>

<output>
Create `.planning/phases/02-honest-analyst-depth/02-01-SUMMARY.md` when done. Include:
- What changed in each SQL file (before → after, with line refs)
- Whether `bq` dry-run was run and the results
- Phase 1 assumption verification status (link to PHASE1-ASSUMPTIONS-VERIFIED.md)
- Any unexpected findings (e.g., if a SQL file had drifted from RESEARCH.md's expected starting state)
</output>
</output>
