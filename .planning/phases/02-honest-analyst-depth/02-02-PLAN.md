---
phase: 02-honest-analyst-depth
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .claude/commands/run-analyzer.md
  - runs/README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - ANALYSIS-03
  - ANALYSIS-05
  - REPORT-01
  - REPORT-02
user_setup: []
tags:
  - analyzer
  - recipe
  - report-structure

must_haves:
  truths:
    - "The /run-analyzer recipe reads up to three most recent reports/*.md files before drafting and records the dates consulted in summary.json"
    - "The recipe queries the live eligible video count from video_metadata each run and uses it for confidence-label denominators"
    - "Every pattern claim in the drafted report ends with an inline parenthetical (low|moderate|standard confidence, n=N), or appears in a table with Confidence and n columns"
    - "The drafted report has all six required sections in order: Data Health, Headline, What is working, What is not working, Patterns worth watching, Open questions"
    - "When a section has no findings or its findings would draw from a stale table, the section heading still renders and the body explicitly says what is missing and why (D-11/D-12)"
    - "summary.json includes a prior_reports_consulted field (D-10)"
  artifacts:
    - path: .claude/commands/run-analyzer.md
      provides: "Extended recipe with prior-report read step, reworked draft step that applies CLAUDE.md rules, six-section structure with disclaimers, inline confidence labels"
      contains: "Step: Read prior reports for calibration"
    - path: runs/README.md
      provides: "summary.json schema documentation extended with prior_reports_consulted"
      contains: "prior_reports_consulted"
    - path: CHANGELOG.md
      provides: "Audit entry for the recipe extension and summary.json schema change"
      contains: "recipe"
  key_links:
    - from: ".claude/commands/run-analyzer.md (draft step)"
      to: "CLAUDE.md sections (Age control, Small samples get hedged, Report structure, Brutal honesty)"
      via: "Section-title references in recipe prose; never §N numeric references"
      pattern: "CLAUDE.md § \""
    - from: ".claude/commands/run-analyzer.md (prior-report read step)"
      to: "reports/*.md archive"
      via: "ls reports/ | grep date | sort | tail -n 3"
      pattern: "reports/"
    - from: ".claude/commands/run-analyzer.md (eligible-count step)"
      to: "video_metadata table via bq query"
      via: "Inline bq query using the LEAST-snapshot CTE pattern fixed in Plan 01"
      pattern: "eligible_count"
---

<objective>
Extend Phase 1's `/run-analyzer` recipe with three new behaviors that turn CLAUDE.md's analytical contract into executable steps: (a) read the three most recent prior reports before drafting and record which were consulted, (b) query the live eligible video count and apply CLAUDE.md's confidence thresholds to every pattern claim, and (c) draft the full six-section report with inline confidence labels, stale-table disclaimers in dependent sections, and an empty-section policy that never silently omits a heading.

Purpose: CONCERNS.md flagged that "there is no executable code that enforces age control, small-sample hedging, or the report structure" — every rule is prose. D-01 fixes Layer 1 of the enforcement by re-referencing CLAUDE.md sections at draft time. Without this plan, Phase 2 ships a recipe that still relies on Claude remembering to apply rules; with it, the recipe makes the rules unmissable.

Output:
- An extended `.claude/commands/run-analyzer.md` with the three new behaviors wired into the existing draft → publish flow
- `runs/README.md` updated to document the new `prior_reports_consulted` field in `summary.json`
- A CHANGELOG.md entry recording the analyzer-behavior change
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-honest-analyst-depth/02-CONTEXT.md
@.planning/phases/02-honest-analyst-depth/02-RESEARCH.md
@.planning/phases/02-honest-analyst-depth/02-01-SUMMARY.md
@.planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md
@CLAUDE.md
@BUSINESS_RULES.md
@reports/README.md
@runs/README.md
@docs/maintenance.md
@sql/02_top_full_length_videos.sql
@sql/03_age_controlled_performance.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add prior-report read step and eligible-count step to the /run-analyzer recipe</name>
  <read_first>
    - .claude/commands/run-analyzer.md (if exists from Phase 1; the file Plan 02 edits in place)
    - .planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md (Plan 01 output; tells this task whether Phase 1 has shipped a clean draft → publish seam to insert between, or whether this task must also create the seam)
    - .planning/phases/02-honest-analyst-depth/02-RESEARCH.md (sections "Architecture Patterns" diagram showing where the new steps go; "Pattern 4" for the eligible-count query shape; "Prior-report read step" code example; "Open Question 4" — same-day retries are filtered out before taking top-3)
    - CLAUDE.md (sections "Small samples get hedged, every time" for the confidence thresholds the eligible-count drives; "Report structure" for the prior-report read rule; "Persistent structure" for the reports/ archive contract)
    - reports/README.md (the analyzer-memory rule that ANALYSIS-05 implements)
    - sql/02_top_full_length_videos.sql, sql/03_age_controlled_performance.sql (Plan 01 outputs; the recipe references these as the canonical SQL files and the eligible-count query borrows the same latest_common CTE pattern)
  </read_first>
  <action>
    Edit `.claude/commands/run-analyzer.md` in place. Do NOT create a new file or split the recipe across multiple files (per Phase 1 D-02, the recipe is a single linear markdown file).

    **If `.claude/commands/run-analyzer.md` does not yet exist** (Phase 1 hasn't shipped at execution time): coordinate via PHASE1-ASSUMPTIONS-VERIFIED.md. Stop and surface the blocker rather than scaffolding a placeholder recipe — Phase 2's edits assume Phase 1's draft → publish seam already exists, and inventing one risks divergence.

    **If the file exists**, insert two new sections according to the architecture diagram in 02-RESEARCH.md:

    **New section A — "Step: Read prior reports for calibration"** — insert BEFORE the existing "Draft report" step.

    Required content for this section:
    - One-line purpose statement: this step implements ANALYSIS-05 and D-08 from `.planning/phases/02-honest-analyst-depth/02-CONTEXT.md`.
    - Shell-style instruction to list reports excluding today's date: list files matching the `YYYY-MM-DD.md` (or `YYYY-MM-DD-N.md`) pattern under `reports/`, exclude any whose date prefix matches today's run date (RESEARCH.md Open Question 4), sort lexicographically, take the last 3. Use the pattern from RESEARCH.md's "Prior-report read step" code example as the model; the recipe should be unambiguous prose plus a literal command, not pseudocode.
    - Three bullets explaining what the analyzer does with the prior content: upgrade or downgrade confidence labels if the eligible set has grown or shrunk; avoid restating prior findings verbatim (fresh-frame the same patterns); notice regressions.
    - Explicit no-citation rule per D-08: the prior reports are read for calibration; the new report does not cite them. Cross-week framing is allowed per D-09 only when self-contained (example: `"For the third consecutive week, X has held."`); banned phrases include `"as we said last week"`, `"as noted previously"`, `"the prior report"`.
    - Zero-or-few-priors handling: if fewer than three reports exist (or none), read what is there (which may be zero) and continue; do not block.
    - Output: record the dates actually consulted as a JSON array of `YYYY-MM-DD` strings written to `summary.json.prior_reports_consulted` (D-10). If zero were consulted, write `[]`. The recipe step instructs the analyzer to hold this list until the persistence step at the end of the run.

    **New section B — "Step: Query live eligible video count"** — insert AFTER section A and BEFORE the existing "Draft report" step.

    Required content for this section:
    - One-line purpose statement: this step implements ANALYSIS-03 and D-07 — the `N` in `(label, n=N)` parentheticals comes from a live count each run, never hardcoded (CLAUDE.md § "Small samples get hedged" rule).
    - The inline `bq query` to run (per RESEARCH.md Pattern 4 and Open Question 3 default: no new SQL file). The query computes `eligible_count` as the count of full-length videos at the latest common snapshot whose `days_since_published >= 14`, alongside `total_full_length`. Use the same `latest_common` two-subquery LEAST CTE pattern Plan 01 added to sql/02 (do not extend to four tables; same D-06 scope).
    - Instructions to derive confidence label from `eligible_count`: per CLAUDE.md § "Small samples get hedged", `< 5` → `low confidence`, `5 to 10` → `moderate confidence`, `>= 10` → `standard confidence`. Verified A6 in 02-RESEARCH.md confirms the exact boundaries: `n=4` → low, `n=5` → moderate, `n=10` → standard. Cache the channel-wide eligible count for the duration of the run; sub-population claims scope their own counts (RESEARCH.md Pitfall 2).
    - Persist the raw query result to `runs/{run_date}/queries/eligible_video_count.json` per BQ-02-style convention so the audit trail captures the denominator.
    - Output: an in-memory record `{eligible_count: N, total_full_length: M, latest_common_snapshot: "YYYY-MM-DD"}` that the draft step consumes.

    Place both new sections under appropriately leveled markdown headings (most likely `## Step: ...`) matching Phase 1's existing heading style. Do not modify Phase 1's existing data-health step, canonical-SQL pull step, or `write-notion-report` invocation.

    These two new sections implement the Plan 02 truths and key_links: prior-report read (ANALYSIS-05, D-08, D-10), and live eligible-count (ANALYSIS-03, D-07 supporting infrastructure).
  </action>
  <verify>
    <automated>test -f .claude/commands/run-analyzer.md &amp;&amp; grep -q '^## Step: Read prior reports for calibration' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'prior_reports_consulted' .claude/commands/run-analyzer.md &amp;&amp; grep -qE 'eligible[_ ]count|eligible video count' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'CURRENT_DATE("America/Phoenix")' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'latest_common' .claude/commands/run-analyzer.md &amp;&amp; grep -qE 'low confidence|moderate confidence|standard confidence' .claude/commands/run-analyzer.md &amp;&amp; ! grep -E 'BUSINESS_RULES\.md §[0-9]' .claude/commands/run-analyzer.md</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/run-analyzer.md` exists (if it did not before this task and Phase 1 was confirmed not-yet-shipped, this task stopped and surfaced the blocker; otherwise the file exists).
    - The file contains a heading `## Step: Read prior reports for calibration` whose body references `reports/`, the `tail -n 3` selection (or equivalent take-last-3 logic), the `prior_reports_consulted` summary.json field, and the no-citation rule with at least one banned phrase (e.g., `"as we said last week"`).
    - The file contains a heading whose body describes the eligible-count step and includes a literal `bq query` command with `CURRENT_DATE("America/Phoenix")`, the `latest_common` LEAST-CTE pattern, and a `>= 14` age filter.
    - The file mentions all three confidence-label tiers (`low confidence`, `moderate confidence`, `standard confidence`) and ties them to the CLAUDE.md sample-size thresholds by section title (not §N).
    - The two new sections appear in the recipe before the draft step (i.e., earlier in the file than wherever the existing draft step heading is).
    - No `BUSINESS_RULES.md §N` numeric-section references appear anywhere in the modified file.
  </acceptance_criteria>
  <done>
    The recipe now reads prior reports and queries the live eligible count before drafting. The analyzer has the data it needs to apply confidence labels correctly in the next task. The prior-report read step records which dates were consulted for the audit trail.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rework the recipe's draft step to apply CLAUDE.md rules explicitly and produce all six sections with inline labels and stale-table disclaimers</name>
  <read_first>
    - .claude/commands/run-analyzer.md (current state including Task 1's additions; this task edits the existing draft-report step in place)
    - .planning/phases/02-honest-analyst-depth/02-RESEARCH.md (sections "Architecture Patterns" Step 4; "Pattern 5" for inline parenthetical format and table format; "Common Pitfalls" 2 and 5 for the per-claim `n` scope rule and the stale-table-disclaimer pile-up collapse rule)
    - CLAUDE.md (sections "Voice", "Brutal honesty about underperformance", "Age control is non-negotiable", "Small samples get hedged, every time", "Never claim what the data does not support", "Report structure" — the recipe references these BY SECTION TITLE for the analyzer to load at draft time)
    - .planning/phases/02-honest-analyst-depth/02-CONTEXT.md (D-07 inline parenthetical format; D-07a single-claim-single-label; D-07b no Notion callout; D-09 cross-week framing rules; D-11 empty-section contract; D-12 stale-table disclaimer in dependent section)
  </read_first>
  <action>
    Edit the existing draft-report step in `.claude/commands/run-analyzer.md` (do not insert a new step; the existing step expands).

    Rewrite the draft step's instructions so the analyzer is given an explicit rule-application checklist BEFORE writing prose. The recipe must enumerate each rule by CLAUDE.md section title; the analyzer is responsible for the application. Do not paraphrase the rules — reference them by name so a future CLAUDE.md edit is reflected automatically (per D-01 Layer 1).

    The reworked draft step contains, in order:

    **1. Rules to apply (a bulleted list of section-title references):**
    - Apply CLAUDE.md § "Age control is non-negotiable" — exclude any video with `days_since_published < 14` from top-performer claims and pattern claims; flag as low-confidence if mentioned at all. Use the first-30-day window when possible; the `views_per_day_since_publish_proxy` from `sql/03_age_controlled_performance.sql` is a proxy and must be labeled as such per the SQL file's own header comment.
    - Apply CLAUDE.md § "Small samples get hedged, every time" — use the eligible_count from Task 1 to assign labels (`< 5` → low, `5 to 10` → moderate, `>= 10` → standard). For sub-population claims, scope `n` to that sub-population (RESEARCH.md Pitfall 2 example: tutorial-only claim cites `n=7`, not the channel-wide `n=18`).
    - Apply CLAUDE.md § "Brutal honesty about underperformance" — equal weight to wins and misses; do not bury weak results in caveats; name videos and numbers.
    - Apply CLAUDE.md § "Never claim what the data does not support" — distinguish observed / inferred / assumed; if data did not move enough to be meaningful at this sample size, say so.
    - Apply CLAUDE.md § "Voice" — first-person plural where it fits; no em dash (U+2014); no en dash (U+2013) as punctuation; no banned vocabulary from the project CLAUDE.md list (delve, leverage, robust, seamless, navigate, underscore, showcase, tapestry, realm, multifaceted, transformative, testament to); no formulaic openers or closers; no contrastive reframes. The full enforcement is in Plan 03's self-audit; the draft applies the rules during writing so the self-audit has less to fix.
    - Apply D-08 from CONTEXT.md — prior reports are calibration memory, not reader memory; do not cite them in prose; cross-week framing per D-09 is allowed only when self-contained.

    **2. Confidence label format (per D-07):**
    - For prose claims: end the sentence with ` (label, n=N).` where label is one of `low confidence`, `moderate confidence`, `standard confidence`. Example from RESEARCH.md Pattern 5: `VID042 "GraphQL vs REST" pulled 4,300 views in its first 30 days, about 5× the channel median of 860 (standard confidence, n=18).`
    - For table-shaped claims: add `Confidence` and `n` columns to the table; do not inline the parenthetical inside cells (D-07).
    - Single claim → single label (D-07a). Do not emit per-section header confidence blocks. Different claims in the same section may carry different labels.
    - Do not wrap labels in Notion callouts (D-07b). Callouts are reserved for stale-data flags from the Phase 1 Data Health section.

    **3. Six-section structure (REPORT-01, D-11):**

    Required section order with required heading text:
    - `## Data Health` — already produced by Phase 1's data-health step; the draft step renders it as the first section verbatim from the parsed `sql/04` output. No analyst commentary added here unless a stale table is reported (then a one-line line per stale table naming the staleness in days).
    - `## Headline` — one or two sentences capturing the most important finding of the week. Lead with the finding; no preamble.
    - `## What is working` — two to four findings with numbers, age context, and inline confidence labels. Exclude any video with `days_since_published < 14` (low-confidence flag applies even if briefly noted).
    - `## What is not working` — same shape as "What is working." Do not skip this section if results are not dramatic; smaller misses still matter.
    - `## Patterns worth watching` — early signals labeled with confidence; multi-week framing per D-09 allowed when self-contained.
    - `## Open questions` — things the data hints at but cannot answer.

    Each section heading MUST render even if the section has no material (D-11). Empty-section bodies use one of these explicit forms:
    - `Nothing material to report this week.` (when the data was available but no finding meets the confidence bar)
    - `{Topic} analysis unavailable: {table} is {N} days stale (see Data Health).` (when the section depended on a stale table)

    **4. Stale-table disclaimer rule (D-12, RESEARCH.md Pitfall 5):**
    - If a finding would have drawn from a table flagged as stale by the Phase 1 Data Health step, replace the finding with a one-line disclaimer that names the table and the staleness, and points to Data Health. Do NOT compute against stale data.
    - When multiple findings in the same section would all disclaim the same stale table, collapse to ONE disclaimer per section. Example: `Watch-time and traffic-source analysis is unavailable: daily_video_analytics is 89 days stale (see Data Health).`
    - At planning time `daily_video_analytics` and `daily_traffic_sources` were 89 days stale. The first Phase 2 run is expected to exercise this rule heavily.

    **5. Output dict (NOTION-02 / RESEARCH.md A2):**
    - The draft step ends by assembling the structured report dictionary that the existing `write-notion-report` Skill invocation step consumes. Per PHASE1-ASSUMPTIONS-VERIFIED.md A2: if Phase 1 ships the structured-per-finding shape, populate `confidence: {label: "moderate", n: 7}` per finding; if Phase 1 ships markdown-body-only, the confidence label lives in the prose only.

    Do NOT touch the existing Skill invocation or persistence steps; this task only rewrites the draft step.
  </action>
  <verify>
    <automated>grep -qE '^## (Step: )?Draft' .claude/commands/run-analyzer.md &amp;&amp; grep -c 'CLAUDE.md § "' .claude/commands/run-analyzer.md | awk '$1&gt;=5{exit 0} {exit 1}' &amp;&amp; grep -q 'Data Health' .claude/commands/run-analyzer.md &amp;&amp; grep -q '## Headline' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'What is working' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'What is not working' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'Patterns worth watching' .claude/commands/run-analyzer.md &amp;&amp; grep -q 'Open questions' .claude/commands/run-analyzer.md &amp;&amp; grep -qE '\(label,? n=N\)|\(low \| moderate \| standard confidence, n=N\)|low confidence, n=' .claude/commands/run-analyzer.md &amp;&amp; grep -qE 'stale|disclaimer' .claude/commands/run-analyzer.md</automated>
  </verify>
  <acceptance_criteria>
    - The draft-step section of `.claude/commands/run-analyzer.md` contains at least five `CLAUDE.md § "..."` section-title references (Age control, Small samples, Brutal honesty, Never claim, Voice, Report structure — at least five of these six).
    - The draft step lists all six required section headings (`Data Health`, `Headline`, `What is working`, `What is not working`, `Patterns worth watching`, `Open questions`) in that order.
    - The draft step contains an example or schema of the inline-parenthetical confidence format (something matching `(label, n=N)` or `(low | moderate | standard confidence, n=N)`).
    - The draft step contains the stale-table-disclaimer rule referencing D-12 and the per-section collapse pattern.
    - The draft step contains the empty-section rule (D-11): heading still renders; body explicitly names what is missing.
    - The draft step explicitly cites D-07a (single claim, single label) and D-07b (no Notion callout for confidence labels), OR the relevant constraints are present without the D-NN citation (single-label-per-claim and no-callout language).
    - No `BUSINESS_RULES.md §N` numeric references; all section references use titles.
  </acceptance_criteria>
  <done>
    A run executing the recipe drafts a report with all six sections present in order, every pattern claim carrying an inline `(label, n=N)` parenthetical or a `Confidence`+`n` column, dependent sections explicitly disclaiming stale tables, and empty sections rendering their headings with explicit no-material lines. The voice rules are applied during writing (Plan 03's self-audit will catch any that slipped).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend runs/README.md summary.json schema with prior_reports_consulted and log the recipe change in CHANGELOG.md</name>
  <read_first>
    - runs/README.md (current summary.json schema documentation; the new field goes inline in the JSON example block, alphabetized or grouped with the other audit fields)
    - .planning/phases/02-honest-analyst-depth/02-RESEARCH.md (section "Extended summary.json schema" — the example shape; "Code Examples" — the JSON example)
    - .planning/phases/02-honest-analyst-depth/PHASE1-ASSUMPTIONS-VERIFIED.md (A3 — confirms summary.json is additive-friendly)
    - CHANGELOG.md (current state including Plan 01's 2026-05-25 entry; append a new bullet under the same dated heading rather than creating a duplicate heading)
    - docs/maintenance.md (entry format)
  </read_first>
  <action>
    **Edit `runs/README.md`:**

    1. In the `## summary.json schema` section's JSON example block, add a new key `prior_reports_consulted` after `notion_page_id` and before `errors` (or wherever fits the existing visual grouping). Value should be an example array of three ISO dates, mirroring RESEARCH.md's example: `["2026-05-18", "2026-05-11", "2026-05-04"]`. The field is part of the audit trail; document below the JSON block (one sentence) that the value is the list of dates of `reports/{YYYY-MM-DD}.md` files read during the prior-report calibration step (D-10), and that the array MAY be empty if fewer than three prior reports exist.

    2. Do NOT remove or rename any existing field. This is an additive change per RESEARCH.md A3.

    3. Do NOT add the `voice_audit` field yet — that lands in Plan 03. Plan 02 only documents `prior_reports_consulted`.

    **Edit `CHANGELOG.md`:**

    Append a new bullet under the existing `## 2026-05-25` heading (the one Plan 01 created — same date; this is a same-day analyzer-behavior change, so it shares the H2). If today's date is different from `2026-05-25` (e.g., the plan executes on a later day), create a new dated H2 above existing ones following STRUCTURE.md naming conventions.

    The new bullet should name three things:
    - The recipe file edited (`.claude/commands/run-analyzer.md`).
    - What new behavior the recipe now has: reads three most recent `reports/{date}.md` files for calibration before drafting; queries live eligible video count and applies CLAUDE.md confidence thresholds; drafts all six required sections with inline `(label, n=N)` confidence and stale-table disclaimers in dependent sections.
    - The summary.json schema extension: new `prior_reports_consulted` field documented in `runs/README.md`.

    Per docs/maintenance.md § "Evolving a business rule": note whether this would have changed recent reports' numbers or framing. Answer: no prior reports exist yet (reports/ contains only README.md at planning time), so the change is forward-looking; the first run after this lands will be the first to exercise the new behaviors.
  </action>
  <verify>
    <automated>grep -q '"prior_reports_consulted"' runs/README.md &amp;&amp; grep -q '^## 2026-05-25$' CHANGELOG.md &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -qE 'run-analyzer\.md|recipe' &amp;&amp; awk '/^## 2026-05-25$/{flag=1; next} /^## /{flag=0} flag' CHANGELOG.md | grep -qE 'prior_reports_consulted|prior report'</automated>
  </verify>
  <acceptance_criteria>
    - `runs/README.md` contains the literal string `"prior_reports_consulted"` (quoted JSON key) inside the summary.json schema example block.
    - `runs/README.md` describes (in prose under or near the JSON block) what the field stores, that it is populated by the prior-report calibration step, and that it may be an empty array.
    - The `voice_audit` field is NOT added to `runs/README.md` in this plan (it lands in Plan 03).
    - `CHANGELOG.md` contains a `## 2026-05-25` (or current-date) H2 heading with at least one new bullet beyond Plan 01's three.
    - That bullet references `.claude/commands/run-analyzer.md` or "recipe" and mentions `prior_reports_consulted` or "prior report".
  </acceptance_criteria>
  <done>
    The summary.json schema documentation reflects the new audit field; CHANGELOG.md records the analyzer-behavior change with a one-line entry under today's date.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `.claude/commands/run-analyzer.md` contains the prior-report read step, the eligible-count step, and a reworked draft step that references at least five CLAUDE.md sections by title and produces all six required headings.
2. `runs/README.md` documents `prior_reports_consulted` in the summary.json schema example.
3. `CHANGELOG.md` has a 2026-05-25 entry naming the recipe change.
4. No `BUSINESS_RULES.md §N` numeric-section references appear in any file modified by this plan.

Integration smoke test (only if Phase 1's recipe and Skill exist): run `/run-analyzer` end-to-end. Verify the published report has all six section headings in order, every pattern claim carries `(label, n=N)` (or a Confidence + n column in tables), the Patterns and What-is-not-working sections explicitly disclaim `daily_video_analytics` (89 days stale at planning time), and `runs/{date}/summary.json.prior_reports_consulted` is present (value will be `[]` if no prior reports exist yet).
</verification>

<success_criteria>
- The /run-analyzer recipe reads prior reports and records the consulted dates (ANALYSIS-05).
- The recipe queries the live eligible video count each run, never hardcodes it (ANALYSIS-03).
- The recipe instructs the analyzer to apply every pattern claim with an inline `(label, n=N)` parenthetical sourced from the live count (ANALYSIS-03, REPORT-02).
- The recipe enumerates the six required section headings in order and the empty-section / stale-disclaimer rules (REPORT-01, D-11, D-12).
- The summary.json schema in runs/README.md documents `prior_reports_consulted` (D-10).
- CHANGELOG.md has a dated entry recording the analyzer-behavior change.
</success_criteria>

<output>
Create `.planning/phases/02-honest-analyst-depth/02-02-SUMMARY.md` when done. Include:
- Final shape of the three new/extended recipe sections (heading paths + one-line purpose each)
- Whether Phase 1 had already shipped `.claude/commands/run-analyzer.md` at execution time (the verified A1 status from Plan 01 vs. what was actually true at execution)
- Whether the structured-per-finding confidence shape (A2) was usable, or whether confidence labels live in prose only
- Any unexpected drift between RESEARCH.md's assumed recipe seam shape and Phase 1's actual recipe shape (so Plan 03 knows what to expect when it inserts the self-audit step)
</output>
