# Channel Patterns Analyzer Python dependencies.
#
# The analyzer itself uses only Python stdlib (csv, json, argparse, datetime,
# zoneinfo, pathlib, random). There is no `pip install -r requirements.txt`
# step in the operator workflow.
#
# External CLI tooling lives outside this manifest:
#   - `bq` / `gcloud` (Google Cloud SDK). See README for install.
#   - `git`, `jq`. Standard Unix utilities.
#
# If a future change adds a real Python dependency, list it below AND update
# CHANGELOG.md naming the justification (PROJECT.md, section "Constraints").
